## 执行进展（M0、M1 完成，M2 进行中）

> 记录于 M2 落地三分之二之后。**只写可核实的事实**，并写明下一步会先撞到什么。

### 已完成

| 批次 | 内容 | 状态 |
| :--- | :--- | :--- |
| **M0.1–M0.9** | 交互补齐（拖拽排序 / 复制粘贴 / 框选 / 空白双击 / 画布菜单 / 缩放 / 折叠持久化 / 排序快捷键 / 4 处瑕疵） | ✅ |
| **M1.1–M1.3** | 6 套主题、解析规则、按文档记忆 | ✅ |
| **M1.4** | **固化当前主题到节点样式**（面板里的显式动作，带确认） | ✅ |

**M1 全部完成。**
| **M2 前置** | **抽出工具栏** `MindmapToolbar.tsx`（280 行 JSX + 两个下拉菜单状态） | ✅ |
| **M2.1** | 逻辑结构图（默认，与原实现逐项一致） | ✅ |
| **M2.2** | **双向**（一级分支按体量分列左右，根居中） | ✅ |
| **M2.4** | **纵向**（根在顶部，层级向下，兄弟横向铺开） | ✅ |
| **M2.3** | **径向**（扇区按体量分配，环半径由相邻间距反推） | ✅ |
| **M2.5** | **时间轴**（一级分支沿轴交替上下，连线沿轴走再垂下） | ✅ |

**M2 的五个布局全部完成。**

| 批次 | 内容 | 状态 |
| :--- | :--- | :--- |
| **M5.1a** | **导出 SVG**（与 PNG 共用同一份"独立 SVG"构造） | ✅ |
| **M5.1b** | **打印 / 导出 PDF**（复用应用既有的打印链路，无新依赖） | ✅ |

**测试**：87 文件 / 1086 项 / 100%。`tsc` 与 `vite build` 均通过。

### 径向布局（M2.3）的三处要点

**扇区权重是混合的，不是纯按体量。** 纯按体量分配听起来对，但它会坏：一个只含单个叶子的分支挨着一个大分支时，它的扇区趋近于零宽，而**两个框不重叠所需的半径 = 框宽 ÷ 角间距** —— **整环会被推到几千像素之外**。混合 50% 的均分把最窄扇区**限制在父扇区的固定比例**，重的分支仍然明显更宽（测试里 5:1 的体量比变成不到 3:1）。

**环半径由两条约束取大**：① 上一环半径 + 两环各自的最坏径向半宽 + 间距；② 本环相邻框的角间距反推出的最小半径（**首尾也算相邻 —— 环是圆不是线**）。

**连线的三种形状按半径旋转**：`step` = 沿父方向出到中间环 → 弧上横移 → 到子节点；`bezier` 用同样两点作控制点；`straight` 就是辐条。中间角取**最短弧**，否则刚好跨过起点的分支会绕大半圈。

**折叠按钮**：径向节点的子节点分布在四周，"在哪条边上"没有答案，因此布局节点新增 `toggleOffset`（节点本地坐标的落点），渲染器优先用它，其余布局仍走 `side`。**这也顺手解决了时间轴问题的一半。**

### M1.4：主题体系的逃生口

主题是**默认值来源**：没设样式的节点跟随主题 —— 这是"换肤瞬时且非破坏"的全部依据。代价是**想"以主题的答案为起点、再手工调一个节点"的人无从下手**，因为那个答案从来没有写进节点。

**固化就是补上这一步**：把当前主题解析后的外观写成这些节点自己的样式，此后它们不再跟随主题。

**它不覆盖任何东西。** `resolveNodeAppearance` 本来就**优先节点自己的值**，所以把解析结果写回去，对节点已经设过的每个字段都是**无操作**。因此这里的确认说明的是「**钉住**」而不是「**会丢失什么**」—— 这条直接写进了函数的注释里，因为把确认文案写对的前提是先想清楚它。

**落点**：节点样式面板的动作区，与删除并列 —— 两者都是"对节点的决定"，而不是"对节点的控件"。批量选中时同样可用；**确认由视图层负责**，面板保持纯展示。

### 布局切换的落地方式

**布局是纯视图层选择**：`layoutMindmap(tree, collapsedIds, layoutId)` 只返回坐标与连线路径，不改树、不写文档、不动节点样式。切换布局 = 重新算一次坐标。

**新增的共享结构**（三个布局共用，避免各自漂移）：

| 函数 | 作用 |
| :--- | :--- |
| `makeLayoutNode` | 由树节点 + 位置装配布局节点。**三个布局共用一份字段清单** —— 否则将来给 `MindmapNode` 加一个样式字段，就会漏掉其中一两个布局 |
| `measureSubtree` / `measureSubtreeWidth` | 纵向 / 横向的子树占位（互为转置） |
| `stackHeight` / `stackWidth` | 一排兄弟占用的高度 / 宽度 |
| `shiftToOrigin` | 布局完成后整体归位到原点（左右两侧的伸展只有在排完后才知道） |
| `buildEdgePath(…, axis)` | 连线形状按轴旋转：`step`/`bezier` 原本假定"从左往右流"，直接用在纵向布局上会**横向穿过下一层** |

**新增字段** `MindmapLayoutNode.side`：`right` / `left` / `bottom`。折叠按钮挂在**子节点实际所在的那条边**上；默认布局全是 `right`，所以旧观感没有任何变化。

### 规模（**方法一致才可比**：`split("\\n").length`）

| 文件 | 行数 |
| :--- | ---: |
| `src/components/MindmapView.tsx` | 2108（抽出工具栏与样式面板前为 2742） |
| `src/components/MindmapNodeStyleMenu.tsx` | 533 |
| `src/components/MindmapToolbar.tsx` | 402 |
| `src/services/mindmapSvgExport.ts` | 119（PNG 与 SVG 共用的导出构造） |
| `src/services/mindmapLayout.ts` | 1244（五个布局与全部几何） |
| `src/services/mindmapService.ts` | 1183（迁移前为 1821） |
| `src/__tests__/mindmap-layouts.test.ts` | 1075 |

**工具栏与节点样式面板都已抽出**，`MindmapView` 从 2742 行降到 **2108**。

| 抽出的组件 | 行数 |
| :--- | ---: |
| `MindmapNodeStyleMenu.tsx` | 533（含 8 张预设表） |
| `MindmapToolbar.tsx` | 402 |

**节点样式面板的抽取有一道前置门槛，值得记下来**：它此前**没有任何覆盖** —— 测试里搜到的 `mindmap-ctx` 全都属于**画布（白板）** 的右键菜单，与本面板同名不同物。所以先写了 `mindmap-node-style-menu.test.tsx`（打开面板 → 改背景色/形状 → 同步 → 检查写回 Markdown 的样式注释），**再**搬。

**搬运脚本（`scripts/extract-node-style-menu.cjs`）四次才跑对，每次都是脚本自己的问题**：

1. 把预设表放进了 `return (` 之内 —— 组件文件直接不能解析。**模块级声明与函数体必须分开拼装。**
2. 预设表清单是**手写的五张**，面板实际用了**八张**（边框/文字/连线三套调色板是独立的）。**tsc 抓到的。**
3. 预设表的声明行被**重写成 `const NAME = [`**，丢掉了三条表上的类型标注 —— 于是它们的值退化成 `string`，每一个把值赋给窄类型的控件都报错。**修法不是在这里重述标注，而是把那一行原样搬走。**
4. 图标清单同样是手写的 —— 改成**从原文件的 lucide 导入里按实际使用筛出**（预设表也要参与筛选：有一张表的类型是 `typeof AlignCenter`）。

> 📌 **共同点**：四次都是"**我以为我知道这个文件里有什么**"。脚本每次都在用自己的断言把这些假设变成失败，而不是让它们变成用户看到的 bug —— 这才是先写测试再搬的意义。

**折叠按钮的落点也补上了组件级测试**（默认挂右边缘 / 时间轴挂在上下 / 径向朝外）。

### 布局引擎已迁出（原记录里的先决条件）

坐标、连线、度量、节点装配与三个布局都在 `services/mindmapLayout.ts`；`core/mindmapLayouts.ts` 的 **id 表并入其中**。

**为什么合并**：表、标签与分发是同一份清单的三种视图。分在两个文件里的典型错误是「加进了选择器、忘了加分发」—— 而 `default` 分支会把它**悄悄画成默认布局**，**不会有任何测试失败**。

**依赖只有一个方向**：布局模块读 service 的 `calculateNodeDimensions` 与调色板，service 不知道它存在。

**迁移方式**：`scripts/extract-mindmap-layout.cjs`（**按内容定位而不是行号**，写入前先打印边界供核对，默认 dry-run），沿用本仓库已有的迁移脚本写法。**验证靠默认布局的黄金快照** —— 迁移前后逐项一致。

**死代码扫描**（`scripts/find-dead-code.cjs`）：0 处未用导入、0 处未用样式。

### ⚠️ 四项需要更正的事实

**1. 上一次会话的最后一个提交从未通过一次成功的构建。**

`src/components/MindmapInlineEditor.tsx` 的类型错误（`textAlign?: string` 装不进 CSS 的 `TextAlign`）**引入于最后一次打包之后**：`release/KnowSpace-win-x64` 里的可执行文件停留在 01:01，而该文件改于 01:05。也就是说抽取内联编辑器那一步之后，**没有任何一次 `npm run build` 跑过**，而这个错误会让构建直接失败。

**已修正**（把该 prop 收窄为模型里的 `MindmapTextAlign`），并确认 `tsc` 与 `vite build` 现在都通过。

> 📌 教训与 `package-win.ps1` 开头记的那条同源：**"提交了"与"构建过"是两件事**。该脚本存在的理由就是上一次 dist 落后四小时；这次落后的是源码本身。

**2. 黄金快照必须由 `-u` 生成，不能凭记忆重写。**

本次为默认布局加了三条黄金断言（坐标 / 连线路径 / 包围盒），**在改动任何实现之前**捕获。后来把测试文件重写一遍时，我把其中两条**照记忆手写**了数值 —— 结果那两条"失败"，而失败的是测试文件，不是实现。

**结论**：快照类断言只在**机器生成**时可信；手抄一遍等于把断言变成猜测。

**3. 双向布局的连线段必须按"子节点在哪一侧"选边。**

若按父节点的边取，左侧子节点会从父节点右边缘出发，**穿过父节点自己**。这条已被测试钉住（逐条比对起点与终点的坐标）。

**4. 测试自己也会写错 —— 这是本次第二例。**

径向的扇区测试第一版把「相邻节点角度的中点」当成扇区边界。**这只在两个扇区相等时成立**：不等时，两个扇区被算成同一个数，断言便在**浮点末位**上失败（`expected 1.3264502315156903 to be greater than 1.3264502315156905`），看起来像精度问题，其实是推导错了。

**改法**：换成"两个等重分支夹一个轻分支"的格局 —— 此时**扇区宽度可以直接从可观测的角间距反解**，不需要任何关于边界的假设。

> 📌 与第 2 条同源：**断言失败时，先问是哪一边错了**。这两次错的都是测试。

### 时间轴（M2.5）：方案 ① 可行，但只有一条路

**先否掉了看起来最直接的做法**：从根**直连**分支 —— 直线、`step`、`bezier` 都会**穿过中间的分支方框**。轴是水平的、分支沿轴排开，连向第 5 个分支的线必然掠过前 4 个。

**唯一可行的路是"沿轴走，再垂到节点"**，它成立的前提是：**轴是唯一没有被方框占用的走廊** —— 所有方框按构造都退到轴的一侧至少 `SPINE_GAP`。也正因为这条走廊，**分支必须上下交替**：全放一侧就没有走廊可言了。

**由此得到一条渲染决定**：轴上的每一段**共线且重叠**，若各用各的分支色，靠近根的一段会混成脏色、到远端才渐渐变成单色。因此**轴上的所有段共用第一个分支配色**；分支自己的颜色留在它的节点与子树内部的连线上。

**`side` 增加了 `top`**（时间轴的上侧分支朝上生长）。径向那种"哪条边都不是"的情形仍由 `toggleOffset` 表达 —— 两种机制各管一种"方向不是一个名字"的情况。

**验证**：有一条测试专门断言「**轴上的那段走廊没有任何方框占用**」—— 这是整个设计的支点，它一旦被推翻，时间轴就得改成方案 ②（把主轴提升为渲染概念）。

### 性能实测（方案 §5 风险表里唯一未验证的一条）

方案写着「大图（>500 节点）需实测」。实测两种形状：

| 形状 | 逻辑 | 双向 | 纵向 | 径向 | 时间轴 |
| :--- | ---: | ---: | ---: | ---: | ---: |
| **841 节点 / 3 层**（宽而浅） | 3ms | 3ms | 6ms | 2ms | 1ms |
| **601 节点 / 600 层**（深链，最坏） | 94ms | 124ms | 80ms | **5ms** | 80ms |

**结论：不需要优化。** 真实文档落在第一行的量级上。

**但两行的差距说明了一件事**：每个布局都靠**遍历子树**来回答"这棵子树有多高 / 多宽"，于是一条深度 d 的链要付 d² 次访问 —— 宽树把这份代价完全藏住了。**径向快 20 倍，只因它在第一阶段就把每个节点的尺寸算好、存进条目里复用**，其余四个布局每层都要重算一遍。

> **为什么记录下来而不是顺手优化**：600 层不是任何真实文档的形状，为一个不存在的场景去改四个布局的度量路径，等于**把风险挪到用户真实会走的路径上**。真遇到深文档卡顿，这就是第一个该改的地方。

### ⚠️ 性能测试的断言改过一次：**断言次数，不断言时间**

墙钟上限试过，但它**没活过自己所在的套件**：深链单跑 771ms，62 个文件并行时 **6599ms** —— 那个上限量的是**机器有多忙**，不是代码有多慢。**一个节点被度量多少次，与旁边在跑什么无关**，而那正是代价的构成。

| 形状 | 逻辑 | 双向 | 纵向 | 径向 | 时间轴 |
| :--- | ---: | ---: | ---: | ---: | ---: |
| **841 节点 / 3 层**（度量次数） | 4962 | 5762 | 4121 | **841** | 3281 |
| **601 节点 / 600 层**（度量次数） | 36.2 万 | 54.2 万 | 36.1 万 | **601** | 36.1 万 |

**宽图的天花板定在每个节点 10 次**（实测 3.9–6.9 次）—— 一条"每个节点都重走整棵树"的改动会是它的四十倍。

**深链那条不做断言、只打印**：它按构造就是平方级，任何阈值都是拍出来的数字；它存在的意义是让**两行相差三个数量级**这件事可见。

> ⚠️ **顺带一条工具教训**：这条测试还撞上过 **Vitest 默认的 5 秒超时**（并行时 7.9s）。**故意偏重的用例必须显式给超时** —— 否则它失败的样子看起来像断言挂了，而实际上只是排在了队尾。

### 导出 SVG：一个函数，两种格式

**PNG 导出本来就要先把画布序列化成一份"独立 SVG"再栅格化**，所以 SVG 导出**不需要第二套实现** —— 把那段逻辑抽成 `services/mindmapSvgExport.ts`，PNG 继续拿它去栅格化，SVG 直接把它写进文件。

**共用是刻意的**：两种格式如果各写一份，"某个节点的填充怎么解析"这类修复就会只落在一种格式上，而**两种格式的差异恰恰是那种要等用户拿到一个坏文件才会被发现的问题**。

**它要解决三件事**（各有一条测试）：把平移缩放解掉（那是读者的位置，不是图的一部分）；去掉只属于交互的元素（选择框、添加按钮、缩放手柄）；把来自样式表的颜色落成字面色值（样式表不随文件走）。

**测试还真抓到一个我自己引入的缺陷**：模块里手工设了 `xmlns`，而序列化器**本来就会写它** —— 结果属性出现两次，**那不是合法 XML**，严格的解析器会直接拒绝文件。已改为不手工设置，并用一条断言钉住"命名空间存在**且只有一个**"。

> 附带一条事实：节点自设的颜色经 CSSOM 读回来会变成 `rgb(255, 0, 0)` 而不是 `#ff0000`。**颜色一样、文件也合法**，但测试的预期要按事实写，不能按直觉写。

### 三项待决已获授权，决定如下

用户原话：「需要我拍板的全同意」。三项按下面的选择落子，**理由一并写下**，便于日后推翻时知道当时在想什么。

| 事项 | 决定 | 依据 |
| :--- | :--- | :--- |
| **导出 PDF** | **复用应用既有的打印链路**（`printToPdf` + 打印样式表），**不引入 PDF 库** | 应用早有"高保真 PDF 打印"（Ctrl+P）；为一种**已经能产出的格式**新增依赖，代价不对等 |
| **导入 OPML / FreeMind / .xmind** | **导入为一篇新文档**（走既有的新建文件通道），不替换、不追加到当前文档 | 一篇文档就是一个文件；追加会让两套结构混在一篇里，而结构的事实源是 Markdown |
| **sidecar** | **接受**方案 §7 的 C / B 方案（`<文档名>.mindmap.json`） | 方案已论证；实施随 M3.1 落地 |

### M5.1b：打印即 PDF

**没有写转换器。** 应用已有 `printToPdf`（主进程）与整套 A4 打印样式表，文档的 Ctrl+P 走的就是它 —— 导图复用它，**新依赖为零**。

**要补的是"整张图适配纸张"，原来缺的一半在样式表、一半只能在代码里**：

- **样式表**（`@media print` 内新增一段）：解除平移缩放（`transform: none`）；画布按宽度铺满、高度封顶一页 —— `preserveAspectRatio` 默认 `meet`，所以**过宽或过高的图会被整体缩小而不是被裁掉**；隐藏与 PNG / SVG 导出一致的那批控件。
- **代码**（`MindmapView` 里打印前后设置与复原 `viewBox`）：**`viewBox` 只能由代码给出**，它取决于布局算出的边界，样式表拿不到。监听浏览器与 Electron 打印时都会触发的 `beforeprint` / `afterprint`。
  - 复原值**在打印发生时读取**，而不是在 effect 建立时记住 —— 否则第二次打印会把第一次装上去的值当成"原值"。两条都有测试：视图框换入换出、**连续打印两次也收得回来**。

**一个前提值得写明**：导图是**替换阅读区**的独立视图（`viewMode === "mindmap"`），所以打印导图时纸上只有导图，不会顺带印出文档。

**M0–M2 与 M5.1 都还没有引入任何新文件格式**：五个布局、主题、交互、四种导出全部只作用于视图层或复用既有通道，所以这些里程碑都能随时停下而不留半成品。**sidecar 会是第一个新格式**，这也是它值得单独拍板的原因。

### M3.1 完成：伴生文件落地，节点备注是第一个消费者

方案 §7 的 sidecar 已实施（M3 的其余几项——图标 / 标记 / 标签 / 超链接——都复用同一套通道）。

**通道：两个 IPC，但没有"路径"这个入参。** 渲染进程只报一个**它已经打开的文档路径**，伴生文件名由主进程推导（`<文档>.mindmap.json`），并且只有 Markdown / Canvas 文档才有伴生文件。所以这对 handler **无法被要求读写任意文件**，渲染进程也不需要知道命名规则 —— 这是整个特性的范围约束所在。

**规则（都属于渲染层，且每条都是因为反过来的做法会让人丢掉自己写的东西）**：

| 规则 | 反过来的代价 |
| :--- | :--- |
| **文档优先**：伴生文件**改不动树** | 一份过时或手改坏的文件就能损坏大纲 |
| **缺失 / 读不出 / 解析不了 → 纯树** | 打开文档变成报错，而导图首先只是文档的一个视图 |
| **不认识的段落原样写回**（含更高的 `version`） | 新版本写的东西被旧版本打开一次就没了 |
| **只读不写**：仅打开文档不落盘 | 文件不再等于"读者写了什么"，就没人敢信它 |

**备注（M3.1）**：样式面板顶部一段输入框 + 画布上节点左上角的小标记（`pointer-events: none`，只做指示，不抢点击）。**落盘延迟 600ms**：按字符写盘等于每个字符一次磁盘 IO；面板保持无状态，文字立即进内存、只有写盘等待。
**一处必须写清的正确性细节**：防抖闭包**在设定时就捕获文档键与内容**，而不是在触发时去读 —— 否则"打字后立刻切文档"会把上一篇的备注写进下一篇的文件里，那是唯一一条会**丢字**的路径。视图卸载时会把还没落盘的那次写出去，兜住"打完就关"的情况。

**两处诚实的说明**：

1. ~~**PNG / SVG / PDF 导出里没有备注标记**。导出是从布局数据**重建**的独立图形（不是拷贝 DOM）……~~
   **⚠️ 此条后来被证明是错的，已在下方"更正 + 完成：导出其实一直带着标注"一节中订正。** 结论：导出**拷贝的就是画布 DOM**，标注一直都在文件里；真正缺的是颜色。
2. 防抖窗口内**直接杀进程**（不是正常关闭）仍可能丢最后几个字。这是预算内的取舍：写盘次数与丢失窗口只能换来换去，而正常关闭已经有卸载兜底。

**测试**：三层各测各的 —— 主进程侧用**真实临时目录**测命名与落盘（含"伴生文件绝不碰文档本身"），渲染层测四条规则与桥接降级，组件层测读取 → 标记 → 防抖写入 → 清空 → 无文档键 → 坏文件。

### M3.2 完成：图标库与选取器（同一套通道的第二个消费者）

**图标表是精选的，不是 lucide 全量。** `import * as icons` 会把上千个组件拉进包里，换来的却是一个没法搜索的选取器；而 **lucide 并不是稳定界面** —— 就在当前这个版本里，`CheckCircle2` / `HelpCircle` / `AlertTriangle` 三个"改名后的旧别名"**已经被删掉**。所以表里的 34 个图标（5 组）是**先对着 `node_modules` 里那份 `lucide-react.d.ts` 逐个核过才写的**，而且 `id` 用稳定的短名（`star` / `circle-help`）而不是组件名：**id 是要写进文件的**，不该随一次重构而变。

**一个节点一个图标**：再点同一个就摘掉 —— 这就是"取消"的入口，不必再为它加一层菜单（面板上另留了一个"清除"按钮，给不习惯试探的人）。**图标画在节点框之外**的左侧边缘：节点尺寸是布局的职责，为了一枚图形把框撑大，会移动整张图的每一个节点 —— 那正是黄金坐标快照存在的意义。

**未知 id 不画、不报错、也不擦掉**。更新的版本写进去的图标名，旧版本打开时只是"这个节点暂时没有图标"；一旦在打开时顺手把它清掉，那份文件就再也回不到新版本了。这条与"不认识的段落原样写回"是同一条规矩在两处落地。

**一处收敛**：图标与备注现在走**同一条编辑路径**（一次 `applySidecarEdit` → 一个防抖 → 一次写盘），"未能保存"的提示也从备注那一段挪到面板头部 —— 两种标注都会失败，说一次就够。

**包体代价**：`App` chunk 从 652.5KB 涨到 655.6KB（约 +3KB，gzip +1.2KB）。这是精选表相对全量导入省下来的量级 —— 也说明了为什么值得维护一张表。

### M3.3 完成：优先级与进度（方案里的"标记"收窄了范围）

**先说范围上的改动**：方案原本把"标记"定为 *优先级 1-9 / 进度 0-8 / 星标 / 旗标*，但**星标与旗标已经被 M3.2 的图标表覆盖**（`star` / `flag` 就在"重点"组）。再做一个平行的"标记组"会让同一个概念有两个入口，所以这次只做了**图标表达不了的那两样**：**优先级 1–9**（数字角标）与**进度 1/8–8/8**（扇形表盘）。这是产品形态上的取舍，记录在此以便推翻。

**没有 0「进度」**：0/8 是一个什么也没说的标记，等价于"没有标记"；选择器用"空着"来表达这件事，而不是画一个空圈。

**为什么标记是一段而不是两段**：`markers` 的形状是 `节点 id → 小对象`，因为**一个节点可以同时戴优先级和进度**。若拆成两个数字映射，同一个节点会在两处各占一个键 —— 而"一个节点一个键"正是这份文件其他段落都在用的形状。

**越界值：丢掉或清除，绝不夹取（clamp）**。手改过的文件或来自更宽刻度的版本可能写着 `priority: 12`；把它"顺手"改成 9，等于**替读者下了一个他没下过的判断**。清除只影响**自己那一项** —— 越界的进度不会连优先级一起带走。

**同一条规矩再往下一层**：条目标里**不认识的字段照样保留**（例如 `{"priority": 1, "review": "pending"}`），与"不认识的段落原样写回"是同一条规矩在两级上的落地。

**表盘的几何是被测过的，不是调出来的**：14 像素下画错表盘和画对表盘长得几乎一样，所以测试直接断言**边缘点落在圆上**、**角度等于这一格**（十二点起顺时针）、**大弧标志在过半时为 1**、**扫掠方向恒为 1**。为此把路径算法放进了核心表（纯函数）而不是留在组件里。

**画在哪里**：进度与优先级画在**节点框之上、右上角**，理由与图标一致 —— 节点尺寸属于布局，为标记撑大框会移动整张图。

**两条测试是我自己写错的**（都被实现纠正过来，值得记一笔）：①我以为"越界的进度"应该把整个节点标记清空，实现只清它自己那一项 —— 实现是对的，测试改了；②路径断言里我手数 token 下标，漏算了 `M` / `L` / `A` 这些字母，于是断言读到了 `"A"` —— 改成按名字取弧标志。**第二处尤其说明问题：手数下标写出来的断言，会以"通过"或"失败"任一方式骗人。**

### M3.4 完成：标签（以及承诺过的"第四种形状"复盘）

**先兑现承诺：加第三种形状（`tags`）之前回头看那套 `KNOWN_SECTIONS`，结论是保留、但把判断写进注释。** 理由：**只有"读"是按形状定制的**，而"空就省略 / 是否全空 / 不认识的段落带过去"三件事本来就与形状无关。四种读法各自类型与规则都不同（字符串映射、对象映射、数组映射），把它们塞进一张"描述表"只会让四个不同的函数共用同一个签名，买不到什么。**所以留下"名字列表 + 按形状的读函数"，并写明：再出现一种形状（日期、带标签的链接、嵌套列表）时，该换的是这套做法本身，而不是再加一个名字。**

**标签的规则（这是这次真正要定的事）**：

| 规则 | 理由 |
| :--- | :--- |
| `#` 是装饰，存进去之前剥掉 | 否则会同时存在 `api` 与 `#api` 两枚标签 |
| 按 `,` `，` `、` 与空白切分 | 只认一种分隔符，会把后面整串静默变成一个标签 |
| **大小写不同算同一个**，留下**先写下的那个拼法** | `#API` 与 `#api` 是一枚标签；全部转小写虽整齐，却会毁掉 `#KnowSpace` 的写法 |
| **读取时过同一套规则** | 手改过的文件不该能塞进一个面板自己会拒绝的标签 |
| 建议芯片 = **本文档在用的词表** | 用过的词最可能是下一个要用的，这也是"同一件事两个名字"长不出来的原因 |

**画在节点下方**（仍是"框外"，节点尺寸归布局管）：最多 **3 枚 + `+n`** —— 一张图上挂二十个标签就不是图了。芯片里的文字只为**绘制**截断（18 字符），文件里永远是读者自己的拼法。

**面板里唯一的一处状态**：标签输入框的值不是"存的东西"，而是它的**解析结果**。若由解析结果反向控制，刚打下的分隔符会被立刻吃掉（`api ` 变回 `api`，下一个词就粘上去了）—— 所以草稿留在输入框里，**并把节点 id 一起记住**，这样切换节点时半途写的字不会跟过去。回车与失焦都提交。

**平手时按"首次出现"排，不按名字**：`localeCompare` 会把顺序交给 locale，同一份数据在两台机器上给出不同顺序，而读者看不出任何理由。

**这里又踩了一个测试自己的坑，值得点名**：我的 `nodeIdOf` 只在一级子节点里找，找不到就**静默退回第一个子节点** —— 于是两个标签写进了同一个节点，一条本该失败的断言看起来通过了。**修正方式不是"再试一次"，而是：找不到就抛错，并且真的搜整棵树。** 静默兜底会把错误伪装成通过。（这已是 M3.3–M3.4 里第三次"实现是对的、测试是错的"，前两次是越界清除与手数 token 下标。）

### M3.5 完成：节点链接（M3 到此收口）

**三种形式，加一个"第四个答案"**：`https://…`（交给系统打开）、`[[文档]]` / `[[文档#标题]]`（交给阅读器那套解析器）、`#标题`（本文档内跳转）。**读不懂就返回"不是链接"，而不是猜** —— 裸文本 `产品设计` 不算链接（本应用的内部链接写法是 `[[…]]`，猜的话打错字就会跳走），`[[文档|别名]]` **也判为读不懂**：把竖线前那段当成文档名，会把人送去一个他从没提过的文件；面板会说清"不是可识别的链接 + 可用的三种写法"。**原样存下**这一版读不懂的写法，留给读得懂的版本 —— 这正是"存原文而非存解析结果"的理由。

**画布上的标记只给认得出的链接**：徽标的意思是"这里能去别处"，**为去不了的文字挂一个徽标，就是这张图上唯一的一处谎话**。读不懂的文字仍然在面板里显示（连同提示），没有任何东西被藏起来。

**跟随走的是三条既有通道，没有发明第四条**：外壳的 `openExternal`、阅读器的 `onWikiLinkClick`（**App 里已有 `handleWikiLinkClick`，接的是同一个** —— 一个"这个名字是哪个文件"的答案，不是第二个可能与之矛盾的）、以及标题跳转 `onJumpToHeading`（本文档的标题就是这张图的节点，所以 `#标题` 是找一个节点）。**锚点必须整段匹配**（忽略大小写）：**落在附近的链接比不跳更糟**，因为读者不会知道自己错过了。没有可用通道时，"打开"是**禁用并写明原因**，而不是消失 —— 会消失的控件什么也教不会。

**又一处 TDZ，这次的修法值得记**：我的"跟随链接"回调引用了**声明在它之后**的 `tree` 状态 → 整个组件崩，7 条 UI 测试同时失败。修法不是把那 60 行搬到后面，而是**把树作为显式入参**传进去（`handleOpenLink(tree, nodeId)`）—— 更少改动，也更清楚：这个函数需要什么，写在签名上。

**第三次"形状断言过期"之后，做了结构性修法**：那条"空伴生文件的键必须是这几项"的断言，每加一段就读一次错。改成断言**两个位置互相一致**（`Object.keys(emptySidecar())` ↔ `SIDECAR_SECTIONS`）—— 这才是真正要守的不变量；以后再加段落，测试不需要改，而"加了段落却忘了 `emptySidecar`"依然会被抓到。

### M3.6 完成：分支编号（**M3 六项到此全部完成**）

**一条边界决定了整个实现：编号是"画出来的"，不是"写进文字的"。** 树是要同步回文档的东西，把编号写进节点文字，就等于把读者从没打过的编号前缀塞进他的 Markdown；而且**节点框的宽高是布局引擎按文字量的**，编号混进文字就得混进测量，那正是黄金快照存在的意义。所以编号是**画在节点旁的小标签，不是文字的一部分** —— 测试里最能守住这条的是最后一条：**开着编号改一个节点并同步回文档，写出来的 Markdown 里没有任何编号**（三种可能形态都断言了）。

**画在左上角外侧**：这是唯一没被占的位置 —— 备注徽标在角上、标记在右上、图标在更左侧、标签在下方。也顺带绕开了"编号挤进框里会被裁掉或顶出去"的问题（3 级编号 `1.2.3` 比框的内边距宽）。

**编号只认文档结构，不认布局**：算编号的输入是**树**，不是布局结果 —— 所以**折叠分支、切换布局都不可能让编号变化**（函数压根不接收折叠集合，测试直接断言这点）。一条随"当时展开着什么"而变的编号，比没有编号更糟。

**开关按文档记在 localStorage**（与折叠、主题、布局同类），**这是对方案的一处偏离**：方案把编号列为"sidecar 开关"，但它决定的是"这张图怎么画"，而不是"文档带了什么" —— 这正是 sidecar 的界线；M0.7 的折叠也是同样的判断。代价相同：换台机器打开，丢的只是这个偏好。

**⚠️ 此处原本写的是"PNG / SVG 导出不带编号"—— 已订正（见下一节）**：导出拷贝的就是画布 DOM，编号本来就在文件里。

### 更正 + 完成：导出其实一直带着标注，缺的是颜色

**先纠正一处我说错、并且重复过多次的话。** 从 M3.1 起我反复讲"PNG / SVG 导出不带标注"，理由是"导出从布局数据重建"。**事实是：`buildStandaloneMindmapSvg` 拷贝的就是画布 DOM** —— 只剥掉选择框、加号、缩放手柄，再去掉平移缩放、解析节点与标题的填充（该函数 103 行，M2 之前就在）。所以**六种标注一直都在导出文件里**。这个结论我从未读那个函数就下了，而它一直是可以读的。

**真正的缺陷是颜色**：标注在屏幕上靠**样式表**上色，样式表不随文件走。于是实际导出效果是 —— 标签芯片**黑底黑字**（读不出来）、图标在深色节点上**整片黑**（看不见）、角标与编号是黑圆黑字。**画了，等于没画。**

**修法落在既有机制上**：构造器本来就为节点矩形、下划线、折叠圆、标题文字解析填充；现在补一张**声明式规则表**（选择器 → 暗/亮两套字面量 → 可选的字号字重），一次循环应用。**字号与字重也必须写出来** —— 它们同样只活在样式表里。

**为什么用字面量而不是 `getComputedStyle`**：屏幕上那些颜色是 `color-mix(… var(--surface-1))`，没有文档的样式表就求不出值；而且某个引擎算出的颜色记号（如 `color(srgb …)`）不一定被下一个打开文件的引擎认。这与构造器**既有的**节点填充做法一致，而且这里比那儿更站得住：**标注的颜色没有一项是读者可设的**，它们就是应用自己的强调色 —— 写下来是"抄录"，不是"猜"。

**两条新测试，一条管一头**：

1. **单元**：夹具里放上六种**不带任何颜色**的标注，断言导出后每一条都拿到了填充与字号，且**明暗两套值确实不同**（防"两套写成一样"的假通过）。
2. **组件**：真的渲染一张挂满标注的导图，再构建导出文件，断言六种标注**都在文件里**（含编号 `"1"`）—— 这条正是针对我先前那个错误结论，把"从布局数据重建"这条路彻底排除。

**顺带更正一处**：**PDF / 打印从来不是缺口** —— 它打印的就是渲染后的页面，标注与编号一直在。

**教训**：我把"导出不含标注"当作事实重复了五轮，从未打开那个 103 行的函数看一眼。**能读的代码不该靠推断下结论**；这次订正也说明记录的作用 —— 错话写在记录里，就还有被自己抓到的机会。

### M4 开始：关系线（4.2），以及它逼出来的一个真 bug

**先解释为什么不按编号做 4.1。** 4.1 自由主题需要**拖动 + 它自己的文本编辑 + 删除**三套交互；4.2 关系线的创建手势**直接复用已有的两主题 Ctrl 多选**（"对这两个做点什么"而不是"进入一个模式"）。所以关系线是 M4 里**最小的完整切片**。**自由主题紧随其后**，不是被放弃。

**规范对：无向，只存一种顺序。** `a→b` 与 `b→a` 是同一条关系 —— 否则同一条线会被存两次、画两次、只删掉一次。

**几何当作性质来测**：断言**任意两个框之间，两端都落在框边上**（而不只是举例）。理由与表盘几何一致 —— **一条从框内部开始的线看起来像是故意的**，肉眼查不出来。为此我又数错了一次（`M x y Q cx cy ex ey` 是 **6** 个数字，我按 8 个取了 `[6][7]`，取到 `undefined`）；现在的断言会**指名是哪一端不在框上**。这是这个批次里第三次"实现对、测试错"，且同样是**手数下标**。

**画在轮廓之下**（先于连线与节点绘制）且**虚线**：关系是对大纲的补充，实线同粗细会被读成大纲的一部分；`pointer-events: none` 保证它不抢平移与选中的点击。**一端不在画布上就不画这条线**（分支被折叠、标题被改名）—— 但**关系本身一个字也不动**（有测试）。

**导出带得上**：导出规则表为它扩展了「非颜色属性」（`fill: none`、虚线、线宽）—— 否则文件里会多出一个**黑色填充形状**。这是刚修完的那个坑的同一种。

#### ⚠️ 真正的收获：一个从 M3.1 就在的真 bug

写"连上 → 落盘 → 仍然有线"这条测试时发现：**线的状态被清掉了**。诊断（打印写入内容 vs 画面）给出的答案是：

- **写入的内容里有这条关系**（说明状态曾经正确），**画面却没有**；
- 根因：**伴生文件的读取是在编辑之后才落地的** —— `loadSidecar` 的 Promise 回来时执行 `setSidecar(loaded)`，把读者刚做的编辑**覆盖**成 `null`。

**真实后果比"线不见了"严重**：那一刻内存里只剩空文件，**紧接着的自动保存会把文件里其余的标注一起写没**。也就是说，**从 M3.1 起，只要读者在文件读完之前点一下图标/备注/标签，那次编辑会赢一瞬间，然后连同文件里原有的内容一起消失**。窗口只有几毫秒（一次 IPC 读小文件），但它是**丢数据**，不是显示问题。

**修法两头都保**：视图记录**读者碰过哪些段落**，迟到的装载改为**合并** —— `mergeSidecar(文件, 内存, 碰过的段落)`，**碰过的以读者为准、没碰过的以文件为准**。只看一边都会丢东西：丢掉编辑，或丢掉文件里其余全部。

**为什么以前没抓到**：这个 bug 只在"读取尚未落地时就有编辑"的时序下出现，而之前的组件测试都是**先等装载、再动手**（`waitFor` 或先 `settle`）—— 这条新测试是唯一一条**在同一 tick 里既编辑又推进计时器**的，于是把它撞了出来。**测试的时序覆盖不足，和没测是一样的。**

### M4.1 完成：自由主题（画布上的主题，文档不知道）

**决策 2 最纯粹的一次落地**：一个自由主题**只有 id、文字、坐标**，全部住在伴生文件里，**树与文档一个字都不动**。id 形如 `floating-1`，**递增而非随机** —— 同样的操作序列产生同样的文件，这在版本控制的知识库里意味着**可读的 diff**，也意味着这个特性可测。

**尺寸是量出来的，不是存下来的**：框的大小由 `calculateNodeDimensions` 按文字算 —— 改名就会重新成形，而文件里**永远不会存下一个与自身文字矛盾的尺寸**。测量用的是服务里那一份，所以自由主题和旁边的分支**看起来是同一套东西**。

**取景边界必须合并**：布局的边界**只知道树**（它从没听说过自由主题），所以导出、打印页与"适应画布"一律用 `unionBounds(布局边界, boundsOfBoxes(自由主题))` —— 否则**被拖到空白处的那一个，恰恰会从"本要展示它"的图里被裁掉**。这是附加层的必然代价，不是可选的优化。

**虚线成为统一的视觉语言**：关系线与自由主题都是虚线 —— 这个导图用"虚线"表示**"这是你加上去的那一层"**，与文档本身的实线相区分。

**选中状态是**分开的（`selectedFloatingId`，不混进 `selectedNodeIds`）：所有对"选中节点"生效的操作（删除、样式、连线、批量）都假定自己的 id 是节点，**只要有一个忘了这件事，就会作用于错误的对象**；多一份状态比逐个审计便宜。

**拖动监听在 window 上，落盘仍然防抖**：手势一旦开始，指针几乎立刻离开框体 —— **只在框内跟踪的拖动，会把手势丢在读者手离开形状的地方**。移动只写内存状态，文件照旧等那一次停顿（**一次拖动 = 一次写入**）。

**把文字清空即删除**：与段落读取器对"空主题"的判读**完全一致** —— 一处判读、一处实现，屏幕与后来的版本不可能对此有分歧。

**导出**：框与文字由规则表上色（否则文件里是黑框黑字），**选中圈被剥掉**（与节点选中圈同等对待：那是控件，不是画面）。

**这一版没做的**：自由主题**不承载备注/图标/标记/标签**。文件层面它们本来就能挂（那些都以 id 为键），但**面板是按节点做的**，没有地方编辑 —— 所以不画，而不是画了却改不了。这是明确的下一步，不是遗漏。

### M4.3 完成：概要（对一组主题加括号 + 一句说明）

**手势复用了已有的多选**：选中两个或更多主题 → 画布菜单一行"为本组加概要"。与关系线同一个入口、同一套选中，**没有新的模式要学** —— 后面的 4.4 边界也会用同一套。

**括号而不是方框**，这是刻意的：**方框围住一组节点就是"边界"（4.4）**，那是另一件事 —— 边界说的是"这些属于一起"，概要说的是"这一组加起来是这样"。两者可能同时出现在一张图上，**必须一眼分得开**。

**它有自己的 id（`summary-1`），与关系线不同**：关系线的身份就是那一对主题，而概要是可以被改名、被换掉跨度的 —— 这两种变化都不构成"另一个概要"。

**跨度按给定的记，不按范围记**：概要是关于**读者挑中的那几个主题**的。某个分支被移出这一组时，它带走的应该是**括号的含义**，而不是让括号悄悄改口。

**没有做的一项校验，及理由**：概要不检查所跨的主题**是否同级** —— 同级是它的正经用法，但**伴生文件模块里没有树，这是有意的**；为了校验一个手势而造一棵树，会把文件格式和布局耦合起来。因此跨度里某个主题不在画布上时（被折叠、被改名），括号就**按还在的那部分画**，全不在就不画。

**把标签清空不等于删除**（与自由主题相反）：自由主题的**文字就是它本身**，而概要里读者要的是**括号**；括号上没有字，它也仍然在说"这些是一组"。删除是它自己的动作。

**几何当作性质来测**：立在组的右侧、**高度恰好等于组高**、标签在括号之外并纵向居中 —— 与表盘同理，**偏两个像素的括号看起来像是故意的**，肉眼不会有人报。

**顺手做了一处结构收敛**：现在有三种东西会被行内编辑（节点文字、自由主题文字、概要标签），原来是两段并列的条件渲染 —— 改成**一个描述符 + 一个渲染点**，这也让"同时打开两个编辑器"从"不太可能"变成**不可能**。

**导出**：括号与标签由规则表上色（琥珀色，否则回来是黑线黑字），**选中框被剥掉**（与前两种一致：控件不是画面）。伴生文件至此**八段**，往返测试逐段断言。

### M4.4 完成：边界（把一组主题框起来）—— **M4 至此全部完成**

**手势与概要共用，门槛不同**：边界**一个主题就够**（概要是两个）。差别是实质的：**一个主题周围的框在说"这一个自成一体"，那是括号永远说不出口的话** —— 一个主题的括不过是它旁边的一条线。测试直接断言了这个对比：**同一个选中下，边界那行可点、概要那行不可点**。

**方框画在最底层**（连分支线也在它之上）：框是**背景**，它说的是"这一组"，读者要看的是框里的主题。

**方框本身不吃点击，只有标题吃**。这是刻意的：边界可能覆盖画布的大片区域，而**一个吞掉点击的框会让组内的空白既不能拖拽也不能开菜单** —— 而那片空白恰恰是设计上留出来的。

**颜色写进元素本身，而不是交给样式表**。两个后果都是要的：**导出（克隆画布）自然保住颜色**，不必为它写规则；而且**颜色是读者的选择，不是主题的选择**，本来就不该随明暗主题变。

**边界色与图标码的规则相反，且是刻意的**：不认识的**图标 id 不画**（图标是装饰，少画一个不损失什么），不认识的**边界色 id 落回默认**（框本身就是读者要的东西，**无论认不认得那个颜色，框都必须画出来**）。

### 这一轮测试抓到的两件事（都是我的假设错了）

1. **括号落在了边界框的留白里** —— 概要站位于组右侧 14px，而边界在组外留 16px 白，于是两者在同一组上**重叠 2px**：括号看起来像框的一部分。修法不是各自微调数字，而是**把两个常量绑起来**（`SUMMARY_BRACKET_GAP = BOUNDARY_PADDING + 8`），并把"为什么一个动另一个要跟"写进注释 —— 这两个数本来就该有先后关系，只是之前没有第二个数来暴露它。
2. **我假设"挂载时什么都没选"**，而实际**根节点默认是选中的**。所以那条测试改成了**对照**（见上）而不是空选中 —— 比原来的写法更有价值：它断言的是这两个功能**唯一的区别**。

**一处收敛**：概要与边界是**同一种形状**（一串主题 + 一句话），于是读取器合成一个 `readSpanSection` —— **第二个消费者出现的时刻，正是这个形状值得命名的时刻**（这条规矩之前写在注释里，这次真的用上了）。颜色的读取也归到那里：**不是字符串就不算颜色，认不出的 id 原样留着**（与图标同一条规矩：新版本写的调色板，旧版本打开不该把它抹掉）。

**伴生文件至此十段**，往返测试逐段断言。行内编辑器现在共四种东西（节点文字 / 自由主题文字 / 概要标签 / 边界标题），仍是**同一个描述符、同一个渲染点**。

**测试**：990 项 / 81 文件 / 100%。`tsc` 与 `vite build` 均通过；App chunk 679.70 → 684.08 KB。

### 4.1 的待做项完成：自由主题承载标注（五段全部）

**入口是"右键自由主题 → 它自己的面板"，与"右键节点 → 节点样式面板"对称**。没有走"把节点样式面板遮一半"那条路：自由主题**没有层级、没有父节点、没有分支、也没有形状可言**，那个面板里的一半控件描述的是**它不是的东西**。它真正拥有的是与节点**同样的五段标注** —— 所以共用的是那五段，面板只提供自己的标题与那一个动作。

**一处抽取**：图标 / 标记 / 标签 / 链接 / 备注从节点面板整体搬进 `MindmapAnnotationSections`，两个面板共用同一个渲染。"一个读者不该为了『这个有备注』学两套词汇"这件事，在代码里也成立了一次。**"未能保存"的提示**随之搬进这一段（它属于任何一个标注控件，不属于面板头部）。

**没有新开任何一段存储** —— 自由主题的备注写进 `notes["floating-1"]`，与节点的备注同段同构。**文件根本不区分两者**，这正是当初把标注设计成"按 id"的回报：新消费者出现时不需要新的 schema。

**删除自由主题会清掉它名下的标注；删树节点仍然保留 —— 差别在 id 能不能回来**：

| | 树节点 | 自由主题 |
| :--- | :--- | :--- |
| id 从哪来 | **内容决定**（同一标题重新解析得到同一个 id） | **计数器发放**，永不回收 |
| 删掉之后 | 文档可能把它带回来 → **留着笔记** | 永远回不来 → **留着就是孤儿** |

而且那种孤儿会让**文件自称有内容**（`sidecarIsEmpty` 为假），所以清掉它不只是整洁问题。测试钉住了三件事：一起走、**只清它自己的**、以及**跨到它的概要/边界跨度不动**（跨度是"读者挑过的清单"，画面本来就会自己收缩 —— 剪掉那样一份记录，是这个模块在改读者的选择）。

**两处能力差异，都写进了 UI 而不是默默不做**：

1. **`#标题` 链接在自由主题上打不开**，面板按既有方式说明"这一侧没有能打开它的通道" —— 因为锚点要靠**查大纲**来跟，而自由主题不在大纲里。`https://` 与 `[[笔记]]` 照常可用。
2. 装饰同样是共用的：节点图标与备注徽标从视图搬进了 `MindmapMarks`（链接徽标本来就在那里），所以自由主题**穿在与节点相同的位置、用相同的画法** —— 在 DOM 里画出，也就意味着**导出（克隆画布）自然带上**。

**只有一处状态搬了家**：标签输入框的草稿（它是"对存储值的解析"，不是存储值）跟着标签段走，于是**节点样式面板重新变成无状态组件** —— 与它自己的注释一致。

**验证**：990 → **1001 项全绿**（82 文件）。其中**既有 990 项一项未改地通过**，这本身就是"抽取没有改变行为"的证据。tsc 与构建干净；App chunk 684.08 → 686.70 KB。

### 4.2 的待做项完成：关系线的标签 / 箭头 / 形态 / 颜色 —— **M4 至此再无待做**

**先解决"线怎么被选中"**：一条一像素的曲线不是任何人会刻意点中的目标，所以线上盖了一条**同曲线的无形宽命中区**（14px，样式表里 ✓）。**左键选中、右键打开以这条线为主题的画布菜单**（与右键自由主题同一个形状）、**双击写字**——双击既是线也是标签，所以没有标签时也知道该往哪里写。

**箭头这一项藏着一个真冲突，解法值得写下来**：单向箭头需要**方向**，而存储里的一对主题是**规范序**的（否则同一条线能被存两次、画两次、删一次 ✓）。于是 `forward` / `backward` 是**相对存储顺序**的，而**面板把它翻译成两个主题的名字**——按钮上写的是「子节点甲 → 子节点乙」✓✓。读者选的是"从哪个主题到哪个主题"，文件记的是它本来就能表达的东西。**没有为一个 UI 需要而改 schema。**

**一处结构调整**：那 6 个颜色**不是边界的** —— 是"读者手绘标记用的颜色"（边界画框、关系画线 ✓）。所以搬到中立的 `mindmapPalette.ts`（`MINDMAP_MARK_COLORS`），**一行色板组件两处共用**。规矩与 `readSpanSection` 那次一样：**第二个消费者出现时，给共用的东西起名字**。

### 导出里发现的一个真 bug（被既有测试抓住）

原来的规则表**无条件上色** ✗ —— 那会把**读者选的颜色**在导出里抹掉（正是这一轮开始做的功能 ✗）。修法是：**在套用任何规则之前，先记下每个元素自己声明的颜色**，于是**元素自己的颜色永远优先**，而**规则之间仍按顺序互相覆盖**。

第一版修法是错的，被测试当场抓住：我写成"元素已有颜色就跳过"，结果**通用徽标规则覆盖了它后面那条专用规则**（`is-more` 徽标本该赢），一个既有断言立刻红了 ✓。

还有一条子规则：**`currentColor` 不算"有自己的颜色"** —— 没有样式表时它解析成黑色，而那正是这张规则表存在要避免的事；**每个 lucide 图标恰好都带它** ✓（这条也是既有测试逼出来的：图标在导出里本该是 `#a5d8f0`，第一版修法让它留在了 `currentColor` ✓）。

**虚线则相反**：**总是显式写出（含默认值）**，因为导出没有样式表，交给 CSS 的线会变成实线 ✗；**颜色只在选了时才写**，于是默认跟随主题 ✓。两个字段、两条相反的规矩，各有各的理由。

**一处刻意的边界检查**：删掉自由主题时**连到它的线不动**（与概要跨度同规则 ✓）—— 画面本来就会跳过端点不在的线 ✓，而剪掉它是这个模块在改读者的记录 ✓。这条与"清理挂在被删主题 id 上的标注"合在一起，正好把"什么该清、什么不该清"的界线画清楚了：**挂在东西身上的清掉，关于一组选择的记录留着**。

**测试**：+24 项（**1025 项 / 84 文件 / 100%**）。新增覆盖：几何的纯函数性质（标签落在曲线中间、箭头倒钩对称分居两侧、方向随角度旋转）、四项字段的读写与补丁语义（一次改一样、空值收回默认、非关系不动）、以及组件层（命中区、菜单方向按主题名说、写标签、选颜色与形态、导出保住颜色并剥掉"被选中"）。`tsc` 与构建干净；App chunk 686.70 → 693.33 KB。

### M5.2 完成：导入 OPML（**一次翻译，不是一次合并**）

**导入的产物是 Markdown 文本**，而从那一刻起这篇文档就是和其他文档一样的事实源 —— **导入的文件此后再也不会被读取**。这就是决定"导入为一篇新文档"的实际含义：文档是一个文件，把两套大纲并进一篇，会让之后每一个问题（哪一份结构才是真的、哪部分是从哪来的）都没有答案。

**因此这个模块的产物是 Markdown，它的职责是做一个唯一知道"别人家格式长什么样"的地方。**

### 解析器的取舍（都对着真实导出形状写）

| 事 | 做法与理由 |
| :--- | :--- |
| **嵌套** | 按**元素的直接 `outline` 子节点**读，不按文档顺序 —— 拍平是这类解析最典型的错，而它的表现是"每个主题都在，只是都不在该在的位置"，没有人会报错 |
| **标签** | 依次找 `text`（OPML 2.0 标准）→ `title`（有些导出器这么写）→ 元素文字 → `未命名` |
| **空白** | 折成一行：在源文件里这些是属性值，换行合法；但**换行会把列表项截断**，后半段会变成同级主题 |
| **备注 / 链接** | `_note` 与 `note` 两种写法都认 ✓，`url` 作为链接 ✓ |
| **不认识的属性 / `type="rss"`** | **照常当作普通主题导入**。导出过大纲的人要的是那份大纲，不是一份关于他导出器的报告 |
| **BOM** | **在解析器里**剥掉（那是 XML 的问题，放这儿则下一个调用者不必再发现一次）；主进程读取端只负责读字节 |

**最要紧的性质有测试兜着**：`outlineToMarkdown` → `parseMarkdownToMindmapTree` **读回去是同一棵树**（含每个主题的深度 ✓）。理由是这份 Markdown 一秒后就要成为事实源，翻译必须能过得了那个读取器。

### 备注与链接：按**位置**对上，不按文字

标注写进新文档的伴生文件，key 用**文档刚产生的那些 id**。两棵树按位置一起走，理由有两条：这份 Markdown 就是刚从这个大纲写出来的，形状必然对应；而**相邻的两个主题可以同名**，按文字对会分不清。

**形状对不上时停住，而不是继续走** —— 继续走会把错位之后的每一条标注都贴到错误的节点上，那看起来像"导入器记错了"，而不像一次不匹配。

### 通道：对话框由主进程画，文件由主进程读

渲染进程**从不需要给出一个路径**，拿到的是**文本**而不是一个可以写回的句柄。这也是为什么它被允许读一个**不属于本应用的文档**的文件：读者在原生对话框里亲手选的，而这个读路径不会写回去。（与伴生文件的严格限制不矛盾：那条限制针对的是"渲染进程指定路径"。）

**顺带一处收敛**：canvas 萃取与导入现在共用 `createDocumentFromContent`（建文件 → 刷新目录 → 打开会话 → 提示）——**产出文档而不是编辑文档**的功能，此后只有这一处知道那串顺序。

**测试**：+17 项（**1042 项 / 85 文件 / 100%**）：嵌套、标签链、折行、备注链接、陌生属性、BOM、五种失败（空文件 / 坏 XML / FreeMind 的 `<map>` 被明确拒绝）、往返成树、按位置上标注与形状不匹配时停住、文件命名（含分隔符净化）。`tsc` 与构建干净；App chunk 693.54 → 696.75 KB。

### M5.2 第二块完成：FreeMind `.mm` —— 以及一项**因此消失的工作**

**同一套通道的第二个案例验证了一件事**：通道（对话框 + 主进程读取）、落点（新建文档）、命名、标注落盘，全都没动一行 —— 新增的只有**一个纯解析器**。这正是上一轮把"通道"和"格式"拆开的目的。

**与 OPML 的差别全部由格式决定，不是选出来的**：

| | OPML | FreeMind |
| :--- | :--- | :--- |
| 标签 | `text` / `title` / 元素文字 | **`TEXT`**（大写，两种拼法都认） |
| 子节点 | `<outline>` | `<node>` |
| 备注 | `_note` / `note`（纯文字） | **`<richcontent TYPE="NOTE">` 里的一段 HTML** |
| 链接 | `url` | `LINK` |
| 顶层 | 任意多个 | **恰好一个根** |

**备注是 HTML，就按 HTML 读，然后摊平成文字**：段落与 `<br>` 变成换行（它们要去的地方是一个文本框，**标记只会碍事，而段落是备注自己的形状**）；写成纯文字的备注原样返回。

**"恰好一个根"是一个形状差异，所以接口里有了 `root`**：单一根**就是那份大纲** —— 它的文字给文件命名，它的子节点是文档的第一层。之所以把它保留成 `root` 而不是塞进 `topics`，是为了让**它自己的备注与链接有地方去**：文档的根主题**就是**那个导入的根 ✓（有测试钉住"根的备注落在文档根上，不是丢掉"）。

**两项刻意不读的东西，写出来而不是默默丢掉**：FreeMind 的图标集（那是 Java 客户端的图形，在本应用的图标表里没有对应物）与折叠状态（在这里属于读者的会话，不属于文档）。

**格式由内容决定，不由扩展名决定**：同一个导出器给两种格式都写 `.xml` ✓，所以分派看**根元素** ✓；两者都不像时，报错信息**把两种格式都列出来** —— 读者手里拿的是 FreeMind 文件时，"这不是 OPML"不是有用的消息。

### 一项因此消失的工作

方案里第三项是"导入 Markdown 大纲"。查证后发现：**应用本来就手读 Markdown** —— `parseMarkdownToMindmapTree` 的注释与实现都写着它**同时读缩进列表与标题**。也就是说"把一份 Markdown 大纲做成新文档"**就是打开那个文件** ✓，没有可导入的东西。所以这项**不是被我跳过，而是不存在**（已写进方案表格，免得日后有人再查一遍）。

**测试**：+11 项（**1053 项 / 85 文件 / 100%**）：FreeMind 的嵌套与单根、`TEXT` 两种拼法、HTML 备注摊平（含段落与 `<br>`）、纯文字备注、`LINK`、图标与折叠被忽略、无主题时报错、根主题标注落点、往返成树，以及**按根元素分派**（OPML / FreeMind / 都不像时的说法 / 空文件与坏 XML）。`tsc` 与构建干净；App chunk 696.75 → 698.30 KB。

### M5.3 完成：导入 `.xmind` —— **自己写的 DEFLATE 与 ZIP**

你选了"自己写一个实现"，于是这一轮多了两段通用代码：

| 文件 | 是什么 | 为什么自己写 |
| :--- | :--- | :--- |
| `core/inflate.ts` | **raw DEFLATE 解码**（RFC 1951） | 需要它的地方只有一处：从 `.xmind` 里取出 `content.json`。而 DEFLATE 是**有规格的** —— 下面每一行都是 RFC 的一条，且**由平台的 zlib 来验** |
| `core/zip.ts` | **ZIP 目录与取项** | 同上：需要的是"一个压缩包里的一个文件"，而格式就是几百行偏移量 |

**验证方式才是这一轮的关键**：用 `node:zlib` **压缩**、用我的代码**解压**，**逐字节比对** —— zlib 是参考实现，它自己决定用哪种块（stored / 固定哈夫曼 / 动态哈夫曼 / 一个流里混用），也会产出这段代码没见过的输入。**唯一它不会按需产出的情况 —— stored 块 —— 由手工构造**，免得"只有压缩数据"的解码器永远见不到那一条分支。

**ZIP 的两个经典陷阱都处理了，而且都有测试**：

1. **大小以中央目录为准，不信本地头**。流式写入的压缩包会在本地头里写**零**，把真实大小放在数据**之后**的 data descriptor 里 —— 信本地头的读取器会取到零字节**并报告成功**。
2. **目录末尾记录靠反向扫描找**，因为压缩包后面可能跟着**最多 64KB 的注释** —— "在文件末尾"不是一个可以算出来的位置。

**CRC32 加了**：不加的话，一个**未压缩项**里被翻了一位（其它任何环节都不会注意到）会被当作内容交出去。测试直接翻一位要求拒绝；算法用**标准里那个校验值**（`"123456789"` → `0xCBF43926`）钉住，而**表**用一份**不用表**的实现来查 —— 另写一套"CRC32 是什么"的想法只能比出谁对，而**表才是可能写错的地方**。

**输出封顶 64MB**：DEFLATE 能描述的产出远大于它的输入，几百字节能要求几个 GB —— 对这里要读的东西没有任何影响，却让一次**解压炸弹**变成一句提示。

### `.xmind` 的结构取舍

- **第一张画布就是这篇文档**，其余**数出来并讲清楚**（`warning` 一路带到提示里）—— 一篇文档是一棵树，"已导入"而不说这句就是半句真话。
- **浮动主题不导入，并说明理由**：它们没有文件给出的位置，导进来只会全叠在同一个点上。
- **备注**先读 `plain.content`，没有再摊平 HTML 那一份 —— 用的是 **FreeMind 那份同一函数**（一处收敛 ✓）。`href` → 链接。
- **明确不导入**（写出来，而不是让人日后发现）：边界、概要、关系线、标记、标签 —— "它们需要的是 XMind 主题 id 与本应用节点 id 的对应关系，而**这份对应关系树遍历已经建立**，所以它们是**下一次可以读的东西**，不是读不了的东西"。
- **XMind 8 及更早用 `content.xml`**，被驳回时**明说这件事**，而不是当作解析失败。

**通道从"文本"改成"字节"**（base64 过 IPC）：`.xmind` 不是文本，而"这是哪种格式"必须在**字节到手之后**才能判断 —— 于是**主进程不再对字节的含义有任何意见**，这比之前更干净。

**一处被测试逼出来的改进（错在我，改的是消息）**：取不到项时原来只说"没有需要的那一项"，对读者没有任何信息量；现在读取器**说出它在找什么**（"这个 ZIP 包里没有 content.json"）。**断言没错，是消息该改。**

**验证**：本轮触及的三组（`inflate` / `zip` / 导入）**55 项全绿**，`tsc` 与构建通过；全量测试由你在后台单独跑。

### 校验：翻出五个问题（都不在"测试是绿的"里面）

**绿灯不等于没问题** —— 全量测试通过的同时，逐条核对**测试覆盖不到的地方**（主进程的 `.cjs`、三处必须同名的接线、以及我自己上一轮留下的尾巴），翻出五个：

| # | 问题 | 为什么危险 | 处置 |
| :--- | :--- | :--- | :--- |
| 1 | **导入读取没有大小上限** | 对话框里有"所有文件"，选中一个几百 MB 的文件会被**整个读进内存**，再 base64（×1.33）—— 可能直接卡死 | 读**之前**先 stat，超过 64MB 拒绝并说明 |
| 2 | **扩展名检查已经变成多余的** | 它当初的作用是"早点失败"；现在解析器读内容并说结论 —— 而**导出器写了 `.txt` 的读者不该被告知"你这不是大纲"** | 用**真正防住东西的那个**（大小）替掉它 |
| 3 | **`truncated` 算出来但从没被读** | 读取器里的死信息（一条自己定的规矩就是不留死代码） | 删掉；目录列出多少算多少，没列到的项自然"找不到" |
| 4 | **导入后写标注失败会被静默忽略** | 文档建好了、备注没存，而读者不会知道 —— **正是这个项目一直在避免的那类沉默** | 写盘返回 false 时明说"备注与链接没能写入" |
| 5 | **记录里的用例计数是旧的**（1053，而实际已 1081） | 我上一轮**明说过**这个尾巴，这里补上 | 更正为真实值 |

**另外两处盲区被补上**：

- **主进程那条导入读取路径原本零测试** → 补 5 条：base64 字节往返（**按字节比对**，不是按文本）、`.txt` 现在也能读、**大小边界**（断言用的是**导出的那个常量**，所以边界变化时测试跟着变、不会各自漂移）、目录与空路径、文件不存在。
- **`tsc` 不检查 `.cjs`** —— 这是个真盲区（`electron/main.cjs` 约 68KB 全靠人工看）。用 `node --check` 把三个 `.cjs` 全过了一遍 ✓；同时核对了 **IPC 名字在三处（main / preload / types）一致**、返回字段没有旧名残留（不一致只会在运行时炸）。

**我自己的测试也踩了一个坑，一并记下**：`fs.truncate` **不创建文件**，必须先建一个空文件再拉长 —— 稀疏文件是为了"要测的是大小而不是内容"，但这个前提写错了，测试自己失败了。**测试也是代码。**

**校验结果**：**1086 项 / 87 文件 / 100%**，`tsc`、`node --check`、构建全部通过；App chunk 706.05 KB。同时补推了那个一直在本地的提交（`ahead 1`）。

<!-- end progress -->
