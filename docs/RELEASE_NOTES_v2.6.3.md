# KnowSpace v2.6.3

**导图顶栏重排：双栏展开时，两行对齐成一列。**

> **构建更新（2026-09-21）**：本页资产已随源码一同更新 —— 顶栏改为**按实测宽度自行决定一行/两行**（不再用写死的 1800px 阈值：搜索框开闭、多选节点、标题长短现在都算得进去），两行时缩放与导出移到第二行右端、不再空半行；节点上的**链接徽章改为透明底**（只剩箭头与细边，SVG 导出同步）。详见下方「一行还是两行」与「链接徽章」两节。

这是一个补丁版。v2.6.2 之后，在「文件目录 + 大纲同时展开」时顶栏暴露出来的排版问题。**没有改变任何既有文件格式**：`.md`、`.canvas`、伴生文件与 `.xmind` 都与 v2.6.2 完全一致，升级不需要迁移。

---

## 🛠 修复

### 双栏展开时，顶栏看起来是斜的

问题不在留白，而在**换行由谁决定**。顶栏原本是一条**会自己换行的横排**，而中间控制区的 flex 基准宽度是它的**最大内容宽度** —— 等于先把整行占下；于是被挤到第二行的是右侧那一组（缩放 + 导出），它独自靠右、**上方空着半行**，而标题又在两行之间垂直居中。文件目录与大纲一开，顶栏大部分时间就是这个状态。

现在顶栏是一张**网格**：标题一列、控件区一列、看什么 / 带走什么（缩放 + 导出）一列。控件区再分成结构控件与设置控件两块 —— 装不下时，设置那块**整排**落到第二行，并从**控件列的左边缘**起排、可一直伸展到右端：前者让两行读成一列，后者让它不必再挤第三行。缩放与导出留在**第一行右上**，不参与重排。

### 窄档位下顶栏上下贴边

各窄档位此前只调左右内边距，把上下写成了 0 —— 一行的时候够用，两行就贴边。现在统一补回。

### 主题与布局两个下拉看着像被禁用

它们没有边框、还比旁边的按钮矮一截，文字用的又是 `--text-primary`（在没有定义它的主题里会落回一个偏暗的值）。现在整组做成一个控件：边框、按钮同高、正文色。

---

## ✨ 改进

- **一行还是两行，由顶栏自己量出来**：不再看写死的宽度阈值 —— 那个数是「全带文字 + 搜索框开着」时量出来的，于是搜索框没开的多数窗口明明放得下一行却落进两行，而打开搜索框又会从够宽的窗口里溢出去。现在顶栏把六种排法（单行/两行 × 全文字/收次级标签/收主级标签）各量一遍，**第一种放得下的胜出**；量的是真实内容，标题长短、搜索框开闭、多选徽章、下拉选项宽度都自动算进去，而宽度查询看不见这些。两行时，缩放与导出整组搬到**第二行右端** —— 第二行因此是满的，不再空半行。
- **控件尺寸与间距统一**：控件一律 30px 高（搜索框与缩放读数此前矮一截，看着是"漂"在中间的）；组与组之间 6px、组内 4px —— 间距的分级就是层级的分级。
- 两行版里，结构控件那一行不再以分隔线收尾：它后面不是另一个组，而是换行。
- **节点上的链接徽章改为透明底**：此前是一坨 22% 的紫压在节点角上，链接一多就挂着一串色块。现在底色透明，只剩箭头和一圈细边 —— 徽章读作「这儿有条链」，而不是「这儿有个紫点」；导出的 SVG / PNG 同步。备注徽章仍带底色：它俩分居节点两角，一有底一没底，正好互相认得出。

---

## 🧪 测试基线

**99 个测试套件、1241 项**单元与集成测试 100% 通过（另有 1 项跳过）；`tsc` 干净。布局另有实测：用**真实渲染出的顶栏结构 + 真实样式表**在 1400 / 820 / 1850px 三档截图，并核对计算盒 —— 第二行与第一行第一个控件同列、一行版两个区块不重叠。

---

## 📦 升级

下载本页资产覆盖安装即可，**无需迁移**：文档、画布与伴生文件格式**均未变化**。

- `KnowSpace-Setup-2.6.3.exe` —— 图形化安装程序（推荐）
- `KnowSpace-2.6.3.msi` —— MSI 标准安装包
- `KnowSpace-win-x64-portable.zip` —— 免安装便携版

> 完整说明见 `docs/USER_MANUAL.md`（v2.6.3 同步）。
