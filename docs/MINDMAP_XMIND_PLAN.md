# KnowSpace 思维导图 · XMind 对标方案

> **文档性质**：任务 4 的执行前置方案（先评审，后实施）
> **制定时间**：2026-09-17
> **基线**：`main` @ `df4a4ca`（v2.5.0 已发布）
> **现状来源**：对 `MindmapView.tsx`、`mindmapService.ts`、`mindmapExport.ts` 的逐行勘定（事实清单见 §1）

---

## 1. 现状要点（全部为代码事实）

### 1.1 规模

| 文件 | 行数 | 职责 |
| :--- | ---: | :--- |
| `src/components/MindmapView.tsx` | 2285 | 视图 + 全部交互 + 样式面板 + 导出 |
| `src/services/mindmapService.ts` | 1242 | 数据模型 / 解析 / 序列化 / 同步 / 布局 / 树操作 / 搜索 |
| `src/services/mindmapExport.ts` | 115 | OPML / FreeMind / Markdown 大纲 |
| `src/styles.css`（导图段） | ~1370 | 导图全部样式 |

`MindmapView.tsx` 内含 **25 个 `useState` / 12 个 `useRef` / 29 个 `useCallback` / 7 个 `useEffect`**，单文件 2285 行。

### 1.2 数据模型（`core/types.ts:9-27`）

`MindmapNode` 是**严格树**：`children: MindmapNode[]`，无 `parentId`、无 `isFloating`。

已有属性：`text` · `level` · `line` · `collapsed?` · `color` · `shape` · `lineColor` · `lineStyle` · `fontSize` · `fontWeight` · `textColor` · `borderColor` · `textAlign` · `customWidth` · `customHeight`

**未实现**：备注 · 图标 · 标记(marker) · 标签 · 超链接 · 图片 · 编号 · 字体族

### 1.3 持久化通道（**这是全案的关键约束**）

导图**没有独立文件格式**，唯一持久化方式是把结构写回 Markdown：

| 内容 | 编码方式 |
| :--- | :--- |
| 层级 | 标题 `#` 数量，或列表缩进深度 |
| 顺序 | `children` 数组顺序 |
| 样式 | **行尾 HTML 注释** `<!-- style: color=#10b981,shape=capsule -->` |
| 块引用 | 原有 `^block-id` 保留 |

同步算法 `syncMindmapToDocument` 按**章节（标题块）对齐**，正文段落/代码块/表格/公式**整段搬运**。

### 1.4 已确认的功能缺口（勘定结论）

| 维度 | 缺口 |
| :--- | :--- |
| **结构** | 无自由主题 · 无关系线 · 无概要 · 无边界 |
| **布局** | **仅 1 种**（右向水平树）；无径向 / 双向 / 逻辑图 / 时间轴 / 树状表 |
| **主题** | **无主题概念**（`theme` prop 仅用于 PNG 判定深浅底）；8 色自动分支配色 + 手工选色 |
| **节点内容** | 仅纯文本（无备注 / 图标 / 标记 / 标签 / 链接 / 图片 / 编号） |
| **编辑效率** | 无复制粘贴剪切 · **无同级拖拽排序** · 无框选 · 无空白双击新建 · 无画布右键菜单 |
| **视图操作** | 无缩放快捷键 · **无一键适应画布按钮**（仅挂载时自动执行一次） |
| **状态持久化** | 折叠态 · 缩放态 · 撤销栈**均不持久化** |
| **导入** | **完全没有**（OPML / FreeMind / XMind 均无解析器） |

### 1.5 勘定中发现的既有瑕疵（顺手可修）

1. `reparentNode` **已有 `targetIndex` 形参**，但 UI 调用时**不传** —— 所以拖放一律追加为最后一个子节点。**同级拖拽排序的实现成本因此极低**。
2. `MindmapNode.collapsed` 字段**只被读不被写** → FreeMind 导出的 `FOLDED="true"` 永不生效。
3. `MindmapView.tsx:31,47` 两个**死导入**（`mindmapTreeToMarkdown`、`MindmapLayoutNode`）。
4. `styles.css` 中 `.mindmap-node-add-btn` 等**遗留样式**（JSX 已不渲染，且有测试断言其不存在）。

---

## 2. 核心架构决策（**必须先拍板，否则后面全是返工**）

XMind 的模型是**自由图 + 富节点元数据**；KnowSpace 的模型是**Markdown 文档结构的投影**。两者不是同一种东西，直接堆功能会撞上根本矛盾。以下三个决定是后续一切工作的前提。

### 决策 1 · 富节点元数据（备注 / 图标 / 标记 / 标签 / 超链接 / 图片）存哪里？

| 方案 | 做法 | 代价 |
| :--- | :--- | :--- |
| **A** | 继续扩展 `<!-- style: -->` 注释 | 注释会变成一行 JSON，**Markdown 可读性崩塌**；正文被污染 |
| **B** | 全部搬进 sidecar 文件 | 结构也一起走 → **放弃「Markdown 是结构事实源」这一核心优势** |
| **C** ✅ | **结构 + 样式继续在 Markdown；富元数据进 sidecar** | 需要新增一个伴生文件与同步规则 |

> **推荐 C。** 理由：Markdown 保持「打开任何编辑器都能读懂结构与基本样式」，而备注、图标、标记这类**在纯文本里本来就表达不了**的东西，不该硬塞进注释。
>
> **伴生文件**：`<文档名>.mindmap.json`（与文档同级，仅在该文档确实用到富元数据时才创建 → **不用的人完全看不到它**）。

### 决策 2 · 非树形结构（自由主题 / 关系线 / 概要 / 边界）如何表达？

| 方案 | 做法 |
| :--- | :--- |
| **A** | 坚持纯树，不支持 |
| **B** ✅ | **支持，但只存在于 sidecar** —— 树仍是 Markdown 的投影，自由主题与关系线是**附加层** |

> **推荐 B。** 这保住了两条性质：① Markdown 仍是结构事实源；② 导图能表达 XMind 的图结构。
>
> 具体：自由主题 = 无父节点的顶层节点；关系线 = `{fromId, toId, label?}` 列表；概要/边界 = 任意节点 id 集合 + 样式。**全部只写 sidecar。**
>
> ⚠️ 需明确记录：**导图 → Markdown 写回时，附加层不受影响**（同步只处理树）。

### 决策 3 · 主题（换肤）如何落地？

| 方案 | 做法 | 代价 |
| :--- | :--- | :--- |
| **A** | 切主题时把样式**写进每个节点** | **破坏性** —— 会覆盖用户手工调过的样式，且切回来无法还原 |
| **B** ✅ | **主题 = 视图层默认值；节点显式样式优先** | 需要区分「节点显式设置」与「继承默认」 |

> **推荐 B。** 「未显式设置样式的节点」跟随主题；手工设置过的节点保持不变 → **换肤瞬时且非破坏性**。
>
> 实现要点：主题只影响 `layoutMindmap` 的**默认值来源**（现在硬编码的 `BRANCH_COLORS`、默认字号 15/13、默认形状等），不改写 markdown。

---

## 3. 优先级与分批计划

按「**是否有模型依赖**」与「**用户感知强度**」排序。M0/M1 无模型风险，可立即开工；M3 起需要决策 1/2 拍板。

### M0 · 交互补齐（**零模型改动，收益最高**）

| # | 功能 | 现状 | 实现要点 |
| :--- | :--- | :--- | :--- |
| 0.1 | **同级拖拽排序** | `targetIndex` 形参已存在但未传 | 拖拽时增加「两兄弟之间」的落点判定，传 `targetIndex` |
| 0.2 | **复制 / 粘贴 / 剪切** | 完全没有 | `Ctrl+C/V/X`；剪贴板存子树（含样式）；粘贴为选中节点的子节点 |
| 0.3 | **框选（橡皮筋多选）** | 只有 Shift/Ctrl 点选 | 空白处按下拖拽画选框，与节点包围盒求交 |
| 0.4 | **空白处双击新建节点** | 无 | 双击空白 → 在根节点下新建自由子节点并进入编辑 |
| 0.5 | **画布空白右键菜单** | 只有节点有 | 空白菜单：新建子主题 / 粘贴 / 全部展开 / 折叠至 2 级 / 适应画布 |
| 0.6 | **缩放快捷键 + 适应画布按钮** | 只有滚轮；`handleFitToScreen` 仅挂载时跑一次 | `Ctrl+=` / `Ctrl+-` / `Ctrl+0`；工具栏加按钮 |
| 0.7 | **折叠状态持久化** | 仅内存，重开全展开 | 存 sidecar（跟随决策 1）或 `localStorage` 按文档路径键控 —— **建议前者**，随文档走 |
| 0.8 | **排序快捷键** | 无 | `Alt+↑/↓` 与前一/后一兄弟交换 |
| 0.9 | 顺手修 §1.5 的 4 处既有瑕疵 | — | 死导入、遗留样式、`FOLDED` 死字段 |

**验收**：新增交互全部有测试；折叠态重开后保持；同级拖拽能插到两兄弟之间。

### M1 · 主题体系（**决策 3**）

| # | 功能 |
| :--- | :--- |
| 1.1 | 定义 4–6 套主题（经典 / 简约 / 深色 / 手绘 / 高对比 / 企业） |
| 1.2 | 主题 = **视图层默认值**；节点显式样式优先 |
| 1.3 | 主题选择器（工具栏）+ 主题随文档记忆（sidecar） |
| 1.4 | 「把当前主题固化到节点样式」的显式动作（**可选、有确认**，因为它是破坏性的） |

**验收**：切换主题不改变任何手工设置过的节点；不写坏 Markdown。

### M2 · 布局切换（**无模型改动**）

| # | 布局 |
| :--- | :--- |
| 2.1 | 逻辑结构图（左→右）—— 现有布局，作为默认 |
| 2.2 | **双向**（右 + 左分列，根居中） |
| 2.3 | **径向 / 辐射** |
| 2.4 | 树状表（纵向） |
| 2.5 | 时间轴 |

**关键**：这些**都只是 `layoutMindmap` 的不同实现**，共用同一棵树与同一份 edge 生成逻辑 → 风险低、收益直观。布局选择随文档记忆。

### M3 · 节点富元数据（**需决策 1**）

| # | 功能 | 存储 |
| :--- | :--- | :--- |
| 3.1 | 节点**备注**（富文本/长文本） | sidecar |
| 3.2 | **图标**库 + 选取器 | sidecar |
| 3.3 | **标记**（优先级 1-9 / 进度 0-8 / 星标 / 旗标） | sidecar |
| 3.4 | **标签**（自由文本，可多枚） | sidecar |
| 3.5 | **超链接**（URL / 内部文档 `[[ ]]` / 锚点） | sidecar（内部链接也可考虑直接写进 Markdown 文本） |
| 3.6 | **编号**（自动编号分支，可开关） | 视图层 + sidecar 开关 |
| 3.7 | 字体族选择 | 样式注释（属现有通道，可直接扩） |

### M4 · 非树形结构（**需决策 2**）

| # | 功能 |
| :--- | :--- |
| 4.1 | **自由主题 / 浮动主题**（可拖到画布任意位置） |
| 4.2 | **关系线**（任意两节点间连线，可带标签、可改形态/颜色/箭头） |
| 4.3 | **概要**（对一组同级节点加括号概要） |
| 4.4 | **边界**（把一组节点框起来，可带标题与填充） |

### M5 · 导入导出补齐

| # | 功能 |
| :--- | :--- |
| 5.1 | 导出 **SVG 文件 / PDF**（PNG 已有） |
| 5.2 | **导入** OPML / FreeMind `.mm` / Markdown 大纲 |
| 5.3 | **导入 `.xmind`**（XMind 原生格式，需解析其 zip + content.json） |
| 5.4 | 连线增强：箭头 / 虚线 / 粗细（属渲染层，可提前到 M1） |

---

## 4. 建议的执行顺序与理由

```
M0（交互补齐）  ← 立即可做，无模型风险，用户感知最强
   ↓
M1（主题体系）  ← 无模型风险，视觉提升显著
   ↓
M2（布局切换）  ← 无模型风险，纯布局函数
   ↓
─── 评审点：决策 1 / 2 拍板 ───
   ↓
M3（节点富元数据） ← 需要 sidecar
   ↓
M4（非树形结构）   ← 需要 sidecar
   ↓
M5（导入导出）
```

**为什么 M0 排第一**：勘定显示**五项最影响手感的交互全部缺失**，而它们**完全不碰数据模型** —— 复制粘贴、同级拖拽排序、框选、空白双击、画布右键菜单。其中「同级拖拽排序」的 `targetIndex` **参数早就存在，只是没传**。

**为什么 M3/M4 必须等评审**：它们决定**是否引入 sidecar 文件**，这是**产品形态层面的选择**（用户会不会在文件夹里多看到一个 `.mindmap.json`），不该由我单方面决定。

---

## 5. 风险与回滚

| 风险 | 等级 | 应对 |
| :--- | :--- | :--- |
| **同步算法写坏用户的 Markdown** | 🔴 高 | M0–M2 **不碰 `syncMindmapToDocument`**；M3/M4 涉及的写回改动必须附带「原文保真」回归测试 |
| sidecar 与 Markdown 不一致 | 🟠 中 | 定义明确的合并规则：**Markdown 是树的事实源**，sidecar 只补树表达不了的部分；sidecar 缺失时导图退化为纯树 |
| 2285 行单文件继续膨胀 | 🟠 中 | M1 前先把 `MindmapView.tsx` 按职责拆分（工具栏 / 画布 / 右键菜单 / 编辑层），**否则每加一个功能都在巨石上叠** |
| 主题切换覆盖手工样式 | 🟡 低 | 决策 3 方案 B 从设计上排除 |
| 布局切换影响性能 | 🟡 低 | 各布局共享 measure 阶段；大图（>500 节点）需实测 |

**回滚阶梯**：

```
单批出问题      → revert 该批提交
主题/布局有争议  → 保留数据层，关闭对应 UI 入口
sidecar 方案被否 → M3/M4 整体搁置，M0–M2 不受影响（它们不依赖 sidecar）
```

---

## 6. 本方案的自我约束

- **M0–M2 不引入任何新文件格式**，因此可以随时停下而不留半成品。
- **M3 起才需要决策**，且决策一旦拍板，M0–M2 的成果**不需要返工**。
- 每一批都必须**先加测试再改代码**（沿用本项目已确立的做法），并跑全量 629 项 + 真机确认。

---

## 7. 待你决定的事项

1. **是否接受决策 1/2 的 sidecar 方案**（`<文档>.mindmap.json`）？若否，M3/M4 需要换一种表达方式（或搁置）。
2. **批次优先级是否认可**（M0 → M1 → M2 → 评审 → M3 → M4 → M5）？
3. **M1 前是否先拆分 `MindmapView.tsx`**？我倾向**是** —— 2285 行、25 个 state，直接在上面加 M0 的 9 项交互会让它更难维护。

## M0.7 折叠状态持久化 —— 实现前的两项核实

> 记录于动手之前。这一节只写已经**核实过的事实**和**尚未解决的问题**，不含实现。

### ✅ 核实一：节点 id 跨重载稳定，按 id 持久化是有效的

持久化折叠状态最自然的做法是存节点 id 的集合。这只有在 id 稳定时才成立 —— 若 id 每次解析都重新生成，存下来的集合会在重载后全部落空，而且**单元测试测不出来**（测试里 id 是固定的）。

核查 `parseMarkdownToMindmapTree` 的生成规则：

| 节点来源 | id 形式 | 稳定性 |
| :--- | :--- | :--- |
| 根节点 | `root-mindmap-node` | ✅ 固定字面量 |
| 缩进列表项 | `node-${parentPath}-${childIdx}` | ✅ 由**结构路径**派生 |
| Markdown 标题 | `heading-${line}-${text.slice(0,10)}` | ✅ 由行号与文本派生 |
| **新建节点** | `node-${Date.now()}-${random}` | ⚠️ 随机，但仅存在于当前会话 |

最后一行不构成问题：新建节点在同步回 Markdown 后，下次解析会重新获得**结构 id**。随机 id 是会话内的临时标识。

**结论：可以按 id 持久化。**

> ⚠️ 已知局限（非本次要解决）：id 含**兄弟序号**。若在折叠的节点之前插入同级条目，其序号改变、id 随之改变，折叠会落到错误节点上。这是序号路径的固有代价，换用文本路径可解但会引入重名问题 —— 记为已知取舍。

### ⚠️ 核实二：组件拿不到文档标识，需要一个新 prop

`MindmapViewProps` 现有字段：

```ts
title: string;
headings?: Heading[];
source?: string;
onSourceChange?: (newSource: string) => void;
editable?: boolean;
onJumpToHeading?: (headingId: string, line?: number) => void;
onClose?: () => void;
theme?: ThemeMode;
```

**没有文档路径。** 而持久化的键必须是**每文档一份**。

| 方案 | 评价 |
| :--- | :--- |
| 用 `title` 作键 | ❌ **已知错误** —— 同名文档（如各目录下的 `README`、`索引`）会共用一份折叠状态 |
| 新增 `documentKey?: string`，由 App 传入路径 | ✅ 正确，且 App 已有 `session?.absolutePath` |
| 存进文档自身（frontmatter / 注释） | ✅ 随文档走，但要处理「同步时不写脏」的问题，且会**改动用户的文档** |

**倾向第二个**：不动用户文档，键正确，改动面小（App 一行、组件一行）。

> 之所以把它记下来而不是直接动手：这次加的是**跨文件的新 prop**，属于「开始就必须做完」的改动。上一批 FreeMind 修复是同类的单点改动，可以独立完成；这一项不行 —— 半途而废会在仓库里留下一个「传了键但没读」或「读了但没传」的中间态。

### 尚未核实的事项（动手前需确认）

1. **保存时机**：折叠/展开是高频操作（`handleExpandAll` 一次改掉整个集合），需要与前一批宽度拖拽相同的判断 —— **是否该在每次点击时同步写 localStorage**。宽度那次是拖拽每帧都改所以必须延后；折叠是离散点击，估计可以直接写，但要确认。
2. **失效清理**：文档删改后，存下来的 id 会包含不存在的节点。读取时应**过滤掉树中不存在的 id**，否则集合会无限增长。
3. **与 `FOLDED` 导出共用真源**：`c1582f2` 已让 FreeMind 导出读取该集合。持久化落地后，**导出、渲染、持久化应当读同一份状态**，避免再出现「三处各自理解折叠」的情况。

<!-- end M0.7 -->

## 执行进展（M0、M1 完成，M2 进行中）

> 记录于 M2 落地三分之二之后。**只写可核实的事实**，并写明下一步会先撞到什么。

### 已完成

| 批次 | 内容 | 状态 |
| :--- | :--- | :--- |
| **M0.1–M0.9** | 交互补齐（拖拽排序 / 复制粘贴 / 框选 / 空白双击 / 画布菜单 / 缩放 / 折叠持久化 / 排序快捷键 / 4 处瑕疵） | ✅ |
| **M1.1–M1.3** | 6 套主题、解析规则、按文档记忆 | ✅ |
| **M1.4** | **固化当前主题到节点样式**（面板里的显式动作，带确认） | ✅ |

**M1 全部完成。**
| **M2 前置** | **抽出工具栏** `MindmapToolbar.tsx`（280 行 JSX + 两个下拉菜单状态） | ✅ |
| **M2.1** | 逻辑结构图（默认，与原实现逐项一致） | ✅ |
| **M2.2** | **双向**（一级分支按体量分列左右，根居中） | ✅ |
| **M2.4** | **纵向**（根在顶部，层级向下，兄弟横向铺开） | ✅ |
| **M2.3** | **径向**（扇区按体量分配，环半径由相邻间距反推） | ✅ |
| **M2.5** | **时间轴**（一级分支沿轴交替上下，连线沿轴走再垂下） | ✅ |

**M2 的五个布局全部完成。**

| 批次 | 内容 | 状态 |
| :--- | :--- | :--- |
| **M5.1a** | **导出 SVG**（与 PNG 共用同一份"独立 SVG"构造） | ✅ |
| **M5.1b** | 导出 PDF | ⏸ 需要先定用什么渲染（见下） |

**测试**：63 文件 / 807 项 / 100%。`tsc` 与 `vite build` 均通过。

### 径向布局（M2.3）的三处要点

**扇区权重是混合的，不是纯按体量。** 纯按体量分配听起来对，但它会坏：一个只含单个叶子的分支挨着一个大分支时，它的扇区趋近于零宽，而**两个框不重叠所需的半径 = 框宽 ÷ 角间距** —— **整环会被推到几千像素之外**。混合 50% 的均分把最窄扇区**限制在父扇区的固定比例**，重的分支仍然明显更宽（测试里 5:1 的体量比变成不到 3:1）。

**环半径由两条约束取大**：① 上一环半径 + 两环各自的最坏径向半宽 + 间距；② 本环相邻框的角间距反推出的最小半径（**首尾也算相邻 —— 环是圆不是线**）。

**连线的三种形状按半径旋转**：`step` = 沿父方向出到中间环 → 弧上横移 → 到子节点；`bezier` 用同样两点作控制点；`straight` 就是辐条。中间角取**最短弧**，否则刚好跨过起点的分支会绕大半圈。

**折叠按钮**：径向节点的子节点分布在四周，"在哪条边上"没有答案，因此布局节点新增 `toggleOffset`（节点本地坐标的落点），渲染器优先用它，其余布局仍走 `side`。**这也顺手解决了时间轴问题的一半。**

### M1.4：主题体系的逃生口

主题是**默认值来源**：没设样式的节点跟随主题 —— 这是"换肤瞬时且非破坏"的全部依据。代价是**想"以主题的答案为起点、再手工调一个节点"的人无从下手**，因为那个答案从来没有写进节点。

**固化就是补上这一步**：把当前主题解析后的外观写成这些节点自己的样式，此后它们不再跟随主题。

**它不覆盖任何东西。** `resolveNodeAppearance` 本来就**优先节点自己的值**，所以把解析结果写回去，对节点已经设过的每个字段都是**无操作**。因此这里的确认说明的是「**钉住**」而不是「**会丢失什么**」—— 这条直接写进了函数的注释里，因为把确认文案写对的前提是先想清楚它。

**落点**：节点样式面板的动作区，与删除并列 —— 两者都是"对节点的决定"，而不是"对节点的控件"。批量选中时同样可用；**确认由视图层负责**，面板保持纯展示。

### 布局切换的落地方式

**布局是纯视图层选择**：`layoutMindmap(tree, collapsedIds, layoutId)` 只返回坐标与连线路径，不改树、不写文档、不动节点样式。切换布局 = 重新算一次坐标。

**新增的共享结构**（三个布局共用，避免各自漂移）：

| 函数 | 作用 |
| :--- | :--- |
| `makeLayoutNode` | 由树节点 + 位置装配布局节点。**三个布局共用一份字段清单** —— 否则将来给 `MindmapNode` 加一个样式字段，就会漏掉其中一两个布局 |
| `measureSubtree` / `measureSubtreeWidth` | 纵向 / 横向的子树占位（互为转置） |
| `stackHeight` / `stackWidth` | 一排兄弟占用的高度 / 宽度 |
| `shiftToOrigin` | 布局完成后整体归位到原点（左右两侧的伸展只有在排完后才知道） |
| `buildEdgePath(…, axis)` | 连线形状按轴旋转：`step`/`bezier` 原本假定"从左往右流"，直接用在纵向布局上会**横向穿过下一层** |

**新增字段** `MindmapLayoutNode.side`：`right` / `left` / `bottom`。折叠按钮挂在**子节点实际所在的那条边**上；默认布局全是 `right`，所以旧观感没有任何变化。

### 规模（**方法一致才可比**：`split("\n").length`）

| 文件 | 行数 |
| :--- | ---: |
| `src/components/MindmapView.tsx` | 2108（抽出工具栏与样式面板前为 2742） |
| `src/components/MindmapNodeStyleMenu.tsx` | 533 |
| `src/components/MindmapToolbar.tsx` | 402 |
| `src/services/mindmapSvgExport.ts` | 119（PNG 与 SVG 共用的导出构造） |
| `src/services/mindmapLayout.ts` | 1244（五个布局与全部几何） |
| `src/services/mindmapService.ts` | 1183（迁移前为 1821） |
| `src/__tests__/mindmap-layouts.test.ts` | 1075 |

**工具栏与节点样式面板都已抽出**，`MindmapView` 从 2742 行降到 **2108**。

| 抽出的组件 | 行数 |
| :--- | ---: |
| `MindmapNodeStyleMenu.tsx` | 533（含 8 张预设表） |
| `MindmapToolbar.tsx` | 402 |

**节点样式面板的抽取有一道前置门槛，值得记下来**：它此前**没有任何覆盖** —— 测试里搜到的 `mindmap-ctx` 全都属于**画布（白板）** 的右键菜单，与本面板同名不同物。所以先写了 `mindmap-node-style-menu.test.tsx`（打开面板 → 改背景色/形状 → 同步 → 检查写回 Markdown 的样式注释），**再**搬。

**搬运脚本（`scripts/extract-node-style-menu.cjs`）四次才跑对，每次都是脚本自己的问题**：

1. 把预设表放进了 `return (` 之内 —— 组件文件直接不能解析。**模块级声明与函数体必须分开拼装。**
2. 预设表清单是**手写的五张**，面板实际用了**八张**（边框/文字/连线三套调色板是独立的）。**tsc 抓到的。**
3. 预设表的声明行被**重写成 `const NAME = [`**，丢掉了三条表上的类型标注 —— 于是它们的值退化成 `string`，每一个把值赋给窄类型的控件都报错。**修法不是在这里重述标注，而是把那一行原样搬走。**
4. 图标清单同样是手写的 —— 改成**从原文件的 lucide 导入里按实际使用筛出**（预设表也要参与筛选：有一张表的类型是 `typeof AlignCenter`）。

> 📌 **共同点**：四次都是"**我以为我知道这个文件里有什么**"。脚本每次都在用自己的断言把这些假设变成失败，而不是让它们变成用户看到的 bug —— 这才是先写测试再搬的意义。

**折叠按钮的落点也补上了组件级测试**（默认挂右边缘 / 时间轴挂在上下 / 径向朝外）。

### 布局引擎已迁出（原记录里的先决条件）

坐标、连线、度量、节点装配与三个布局都在 `services/mindmapLayout.ts`；`core/mindmapLayouts.ts` 的 **id 表并入其中**。

**为什么合并**：表、标签与分发是同一份清单的三种视图。分在两个文件里的典型错误是「加进了选择器、忘了加分发」—— 而 `default` 分支会把它**悄悄画成默认布局**，**不会有任何测试失败**。

**依赖只有一个方向**：布局模块读 service 的 `calculateNodeDimensions` 与调色板，service 不知道它存在。

**迁移方式**：`scripts/extract-mindmap-layout.cjs`（**按内容定位而不是行号**，写入前先打印边界供核对，默认 dry-run），沿用本仓库已有的迁移脚本写法。**验证靠默认布局的黄金快照** —— 迁移前后逐项一致。

**死代码扫描**（`scripts/find-dead-code.cjs`）：0 处未用导入、0 处未用样式。

### ⚠️ 四项需要更正的事实

**1. 上一次会话的最后一个提交从未通过一次成功的构建。**

`src/components/MindmapInlineEditor.tsx` 的类型错误（`textAlign?: string` 装不进 CSS 的 `TextAlign`）**引入于最后一次打包之后**：`release/KnowSpace-win-x64` 里的可执行文件停留在 01:01，而该文件改于 01:05。也就是说抽取内联编辑器那一步之后，**没有任何一次 `npm run build` 跑过**，而这个错误会让构建直接失败。

**已修正**（把该 prop 收窄为模型里的 `MindmapTextAlign`），并确认 `tsc` 与 `vite build` 现在都通过。

> 📌 教训与 `package-win.ps1` 开头记的那条同源：**"提交了"与"构建过"是两件事**。该脚本存在的理由就是上一次 dist 落后四小时；这次落后的是源码本身。

**2. 黄金快照必须由 `-u` 生成，不能凭记忆重写。**

本次为默认布局加了三条黄金断言（坐标 / 连线路径 / 包围盒），**在改动任何实现之前**捕获。后来把测试文件重写一遍时，我把其中两条**照记忆手写**了数值 —— 结果那两条"失败"，而失败的是测试文件，不是实现。

**结论**：快照类断言只在**机器生成**时可信；手抄一遍等于把断言变成猜测。

**3. 双向布局的连线段必须按"子节点在哪一侧"选边。**

若按父节点的边取，左侧子节点会从父节点右边缘出发，**穿过父节点自己**。这条已被测试钉住（逐条比对起点与终点的坐标）。

**4. 测试自己也会写错 —— 这是本次第二例。**

径向的扇区测试第一版把「相邻节点角度的中点」当成扇区边界。**这只在两个扇区相等时成立**：不等时，两个扇区被算成同一个数，断言便在**浮点末位**上失败（`expected 1.3264502315156903 to be greater than 1.3264502315156905`），看起来像精度问题，其实是推导错了。

**改法**：换成"两个等重分支夹一个轻分支"的格局 —— 此时**扇区宽度可以直接从可观测的角间距反解**，不需要任何关于边界的假设。

> 📌 与第 2 条同源：**断言失败时，先问是哪一边错了**。这两次错的都是测试。

### 时间轴（M2.5）：方案 ① 可行，但只有一条路

**先否掉了看起来最直接的做法**：从根**直连**分支 —— 直线、`step`、`bezier` 都会**穿过中间的分支方框**。轴是水平的、分支沿轴排开，连向第 5 个分支的线必然掠过前 4 个。

**唯一可行的路是"沿轴走，再垂到节点"**，它成立的前提是：**轴是唯一没有被方框占用的走廊** —— 所有方框按构造都退到轴的一侧至少 `SPINE_GAP`。也正因为这条走廊，**分支必须上下交替**：全放一侧就没有走廊可言了。

**由此得到一条渲染决定**：轴上的每一段**共线且重叠**，若各用各的分支色，靠近根的一段会混成脏色、到远端才渐渐变成单色。因此**轴上的所有段共用第一个分支配色**；分支自己的颜色留在它的节点与子树内部的连线上。

**`side` 增加了 `top`**（时间轴的上侧分支朝上生长）。径向那种"哪条边都不是"的情形仍由 `toggleOffset` 表达 —— 两种机制各管一种"方向不是一个名字"的情况。

**验证**：有一条测试专门断言「**轴上的那段走廊没有任何方框占用**」—— 这是整个设计的支点，它一旦被推翻，时间轴就得改成方案 ②（把主轴提升为渲染概念）。

### 性能实测（方案 §5 风险表里唯一未验证的一条）

方案写着「大图（>500 节点）需实测」。实测两种形状：

| 形状 | 逻辑 | 双向 | 纵向 | 径向 | 时间轴 |
| :--- | ---: | ---: | ---: | ---: | ---: |
| **841 节点 / 3 层**（宽而浅） | 3ms | 3ms | 6ms | 2ms | 1ms |
| **601 节点 / 600 层**（深链，最坏） | 94ms | 124ms | 80ms | **5ms** | 80ms |

**结论：不需要优化。** 真实文档落在第一行的量级上。

**但两行的差距说明了一件事**：每个布局都靠**遍历子树**来回答"这棵子树有多高 / 多宽"，于是一条深度 d 的链要付 d² 次访问 —— 宽树把这份代价完全藏住了。**径向快 20 倍，只因它在第一阶段就把每个节点的尺寸算好、存进条目里复用**，其余四个布局每层都要重算一遍。

> **为什么记录下来而不是顺手优化**：600 层不是任何真实文档的形状，为一个不存在的场景去改四个布局的度量路径，等于**把风险挪到用户真实会走的路径上**。真遇到深文档卡顿，这就是第一个该改的地方。

### ⚠️ 性能测试的断言改过一次：**断言次数，不断言时间**

墙钟上限试过，但它**没活过自己所在的套件**：深链单跑 771ms，62 个文件并行时 **6599ms** —— 那个上限量的是**机器有多忙**，不是代码有多慢。**一个节点被度量多少次，与旁边在跑什么无关**，而那正是代价的构成。

| 形状 | 逻辑 | 双向 | 纵向 | 径向 | 时间轴 |
| :--- | ---: | ---: | ---: | ---: | ---: |
| **841 节点 / 3 层**（度量次数） | 4962 | 5762 | 4121 | **841** | 3281 |
| **601 节点 / 600 层**（度量次数） | 36.2 万 | 54.2 万 | 36.1 万 | **601** | 36.1 万 |

**宽图的天花板定在每个节点 10 次**（实测 3.9–6.9 次）—— 一条"每个节点都重走整棵树"的改动会是它的四十倍。

**深链那条不做断言、只打印**：它按构造就是平方级，任何阈值都是拍出来的数字；它存在的意义是让**两行相差三个数量级**这件事可见。

> ⚠️ **顺带一条工具教训**：这条测试还撞上过 **Vitest 默认的 5 秒超时**（并行时 7.9s）。**故意偏重的用例必须显式给超时** —— 否则它失败的样子看起来像断言挂了，而实际上只是排在了队尾。

### 导出 SVG：一个函数，两种格式

**PNG 导出本来就要先把画布序列化成一份"独立 SVG"再栅格化**，所以 SVG 导出**不需要第二套实现** —— 把那段逻辑抽成 `services/mindmapSvgExport.ts`，PNG 继续拿它去栅格化，SVG 直接把它写进文件。

**共用是刻意的**：两种格式如果各写一份，"某个节点的填充怎么解析"这类修复就会只落在一种格式上，而**两种格式的差异恰恰是那种要等用户拿到一个坏文件才会被发现的问题**。

**它要解决三件事**（各有一条测试）：把平移缩放解掉（那是读者的位置，不是图的一部分）；去掉只属于交互的元素（选择框、添加按钮、缩放手柄）；把来自样式表的颜色落成字面色值（样式表不随文件走）。

**测试还真抓到一个我自己引入的缺陷**：模块里手工设了 `xmlns`，而序列化器**本来就会写它** —— 结果属性出现两次，**那不是合法 XML**，严格的解析器会直接拒绝文件。已改为不手工设置，并用一条断言钉住"命名空间存在**且只有一个**"。

> 附带一条事实：节点自设的颜色经 CSSOM 读回来会变成 `rgb(255, 0, 0)` 而不是 `#ff0000`。**颜色一样、文件也合法**，但测试的预期要按事实写，不能按直觉写。

### 下一步：无待决策项已用尽

**M5.1b（导出 PDF）先要定用什么渲染**：是走系统打印对话框、还是引入一个 PDF 库 —— 这是**依赖层面的选择**，不该由我替你拍板。

**M5.2 / M5.3（导入 OPML / FreeMind / .xmind）方向更大**：导入要先回答"导入到哪里" —— 是替换当前文档、追加到末尾、还是新建一篇，而**导图没有独立文件格式、唯一的持久化方式是把结构写回 Markdown**，所以这个问题的答案会决定导入功能的形态。

**再往后就是 M3 / M4，等 sidecar 裁决**（见下）。

按方案 §4 的顺序，**M2 之后就是决策点**：M3（节点富元数据）与 M4（非树形结构）都需要先定 **sidecar 方案**（`<文档名>.mindmap.json`，见 §7）。

**M0–M2 没有引入任何新文件格式**：五个布局、主题、交互全部只作用于视图层，所以这里可以停，不留半成品。

### M3 的前置状态（未变）

**M3/M4 仍需裁决本方案 §7 的 sidecar 方案**（`<文档名>.mindmap.json`）。M0–M2 不依赖它，可随时停下。

<!-- end progress -->
